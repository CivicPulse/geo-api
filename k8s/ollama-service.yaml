apiVersion: v1
kind: Service
metadata:
  name: ollama
  labels:
    app: ollama
    app.kubernetes.io/part-of: civpulse-geo
spec:
  selector:
    app: ollama
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
  type: ClusterIP
