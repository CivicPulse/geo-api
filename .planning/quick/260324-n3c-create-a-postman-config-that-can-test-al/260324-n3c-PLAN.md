---
phase: quick
plan: 260324-n3c
type: execute
wave: 1
depends_on: []
files_modified:
  - postman/CivPulse_Geo_API.postman_collection.json
  - postman/Local_Dev.postman_environment.json
autonomous: true
requirements: [quick-260324-n3c]
must_haves:
  truths:
    - "Postman collection covers all 8 API endpoints"
    - "Collection can be imported into Postman and requests execute without manual editing"
    - "Environment variables allow switching between local Docker and direct uvicorn"
  artifacts:
    - path: "postman/CivPulse_Geo_API.postman_collection.json"
      provides: "Full Postman v2.1 collection with all endpoints"
    - path: "postman/Local_Dev.postman_environment.json"
      provides: "Environment with base_url and reusable variables"
  key_links:
    - from: "postman collection"
      to: "API endpoints in src/civpulse_geo/api/"
      via: "request URLs and body schemas match actual endpoint contracts"
      pattern: "baseUrl.*geocode|validate|health"
---

<objective>
Create a Postman collection (v2.1 JSON format) covering all 8 endpoints of the CivPulse Geo API, plus a companion environment file for local development.

Purpose: Enable manual API testing and exploration without writing curl commands from scratch.
Output: `postman/` directory with importable collection and environment JSON files.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/civpulse_geo/api/health.py
@src/civpulse_geo/api/geocoding.py
@src/civpulse_geo/api/validation.py
@src/civpulse_geo/schemas/geocoding.py
@src/civpulse_geo/schemas/validation.py
@src/civpulse_geo/schemas/batch.py
@src/civpulse_geo/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Postman collection and environment files</name>
  <files>postman/CivPulse_Geo_API.postman_collection.json, postman/Local_Dev.postman_environment.json</files>
  <action>
Create `postman/` directory at the project root.

**Environment file** (`Local_Dev.postman_environment.json`):
- `baseUrl` = `http://localhost:8000` (default uvicorn)
- `address_hash` = `` (empty, populated by test scripts)
- `provider_name` = `census` (default provider for testing)

**Collection file** (`CivPulse_Geo_API.postman_collection.json`):
Use Postman Collection v2.1.0 schema (`https://schema.getpostman.com/json/collection/v2.1.0/collection.json`).

Organize into 3 folders:

**Folder 1: Health**
1. `GET {{baseUrl}}/health` -- no body, expects 200 with status/database fields

**Folder 2: Geocoding**
2. `POST {{baseUrl}}/geocode` -- body: `{"address": "1600 Pennsylvania Ave NW, Washington, DC 20500"}`, expects 200 with address_hash, normalized_address, results array. Add a Test script that saves `address_hash` from the response into the collection variable: `pm.collectionVariables.set("address_hash", pm.response.json().address_hash);`
3. `POST {{baseUrl}}/geocode/batch` -- body: `{"addresses": ["1600 Pennsylvania Ave NW, Washington, DC 20500", "350 Fifth Avenue, New York, NY 10118"]}`, expects 200 with total/succeeded/failed/results
4. `PUT {{baseUrl}}/geocode/{{address_hash}}/official` -- body: `{"latitude": 38.8977, "longitude": -77.0365, "reason": "Manual override test"}`, expects 200 with official object and source field
5. `POST {{baseUrl}}/geocode/{{address_hash}}/refresh` -- no body, expects 200 with refreshed_providers list
6. `GET {{baseUrl}}/geocode/{{address_hash}}/providers/{{provider_name}}` -- no body, expects 200 with provider-specific result including raw_response

**Folder 3: Validation**
7. `POST {{baseUrl}}/validate` -- body: `{"address": "1600 Pennsylvania Ave NW, Washington, DC 20500"}`, expects 200 with candidates array. Also create a second saved example with structured input: `{"street": "1600 Pennsylvania Ave NW", "city": "Washington", "state": "DC", "zip_code": "20500"}`
8. `POST {{baseUrl}}/validate/batch` -- body: `{"addresses": ["1600 Pennsylvania Ave NW, Washington, DC 20500", "350 Fifth Avenue, New York, NY 10118"]}`, expects 200

For ALL POST/PUT requests: set header `Content-Type: application/json`.

Add a collection-level description: "CivPulse Geo API -- geocoding and address validation endpoints. Run 'POST /geocode' first to populate the address_hash variable used by subsequent geocoding requests."

Add collection-level variables:
- `address_hash` (default empty string)
- `provider_name` (default "census")
  </action>
  <verify>
    <automated>python3 -c "
import json, pathlib, sys
col = json.loads(pathlib.Path('postman/CivPulse_Geo_API.postman_collection.json').read_text())
env = json.loads(pathlib.Path('postman/Local_Dev.postman_environment.json').read_text())
# Check collection schema
assert col['info']['schema'] == 'https://schema.getpostman.com/json/collection/v2.1.0/collection.json', 'wrong schema'
# Count all requests across folders
requests = []
for item in col['item']:
    if 'item' in item:  # folder
        requests.extend(item['item'])
    else:
        requests.append(item)
assert len(requests) == 8, f'expected 8 requests, got {len(requests)}'
# Check environment has baseUrl
env_keys = [v['key'] for v in env['values']]
assert 'baseUrl' in env_keys, 'missing baseUrl in environment'
print('OK: 8 requests, valid schema, environment has baseUrl')
"
    </automated>
  </verify>
  <done>
    - postman/CivPulse_Geo_API.postman_collection.json exists with valid v2.1 schema
    - All 8 endpoints represented with correct HTTP methods, URLs, and request bodies
    - postman/Local_Dev.postman_environment.json exists with baseUrl variable
    - POST /geocode has test script to capture address_hash for downstream requests
    - Collection can be imported into Postman without errors
  </done>
</task>

</tasks>

<verification>
- Verify JSON is valid and parseable
- Verify all 8 endpoints are present with correct methods (1 GET health, 1 GET provider, 3 POST, 1 PUT, 2 POST batch)
- Verify environment file has baseUrl pointing to localhost:8000
</verification>

<success_criteria>
- Both JSON files exist in postman/ directory and are valid JSON
- Collection covers all 8 API endpoints matching the actual route definitions
- Environment file provides baseUrl for easy host switching
- Verification script passes confirming 8 requests and correct schema
</success_criteria>

<output>
After completion, create `.planning/quick/260324-n3c-create-a-postman-config-that-can-test-al/260324-n3c-SUMMARY.md`
</output>
