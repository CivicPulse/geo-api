---
phase: quick
plan: 260329-qm8
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md
autonomous: true
must_haves:
  truths:
    - "All 4 test addresses are geocoded via POST /geocode with trace=true"
    - "Cascade trace shows which stages fired (spell-correct, fuzzy, LLM correction)"
    - "Comparison report shows per-provider results and cascade behavior for each address"
    - "Report highlights differences from prior run (260329-q2v) if any"
  artifacts:
    - path: ".planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md"
      provides: "Full comparison report with provider results, cascade traces, and analysis"
  key_links:
    - from: "curl POST /geocode"
      to: "API cascade pipeline"
      via: "trace=true query param"
      pattern: "cascade_trace"
---

<objective>
Test all 4 addresses against the running local dev environment (http://localhost:8042) with cascade tracing enabled. Generate a comparison report showing how each provider handles the input, what cascade stages activate (spell correction, fuzzy matching, LLM correction), and whether consensus scoring produces a reliable official result.

Purpose: Validate the v1.2 cascading address resolution pipeline end-to-end with real addresses containing intentional typos, abbreviations, and formatting issues. This is a re-run of quick task 260329-q2v — capture current state including any improvements from bug fixes applied in that task.
Output: GEOCODE-COMPARISON-REPORT.md with per-address breakdown of provider results, cascade trace analysis, and overall findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260329-q2v-test-geocoding-providers-and-ai-with-exa/260329-q2v-SUMMARY.md
@.planning/quick/260329-q2v-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Geocode all 4 test addresses with cascade trace and generate comparison report</name>
  <files>.planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md</files>
  <action>
1. Verify the API is running and healthy:
```
curl -sf http://localhost:8042/health
```

If the API is not running, start it with `docker compose up -d` from the project root and wait for healthy status. Check if Ollama/LLM sidecar is running (`docker compose --profile llm ps`). Note the LLM status for the report.

2. Check if the spell dictionary is populated:
```
curl -sf http://localhost:8042/health | python3 -m json.tool
```
Look for spell corrector status in health output or check logs.

3. Geocode each address with `trace=true`. If any return `cache_hit=true`, use the refresh endpoint first (`POST /geocode/{address_hash}/refresh`) to get fresh cascade results, then re-geocode with trace:

```
curl -s -X POST "http://localhost:8042/geocode?trace=true" \
  -H "Content-Type: application/json" \
  -d '{"address": "548 Moss Hl, MacOn GA, 31204"}' | python3 -m json.tool

curl -s -X POST "http://localhost:8042/geocode?trace=true" \
  -H "Content-Type: application/json" \
  -d '{"address": "700 poplar st, Macon, ga, 31201"}' | python3 -m json.tool

curl -s -X POST "http://localhost:8042/geocode?trace=true" \
  -H "Content-Type: application/json" \
  -d '{"address": "659 Arlington place, Macon, ga 3120"}' | python3 -m json.tool

curl -s -X POST "http://localhost:8042/geocode?trace=true" \
  -H "Content-Type: application/json" \
  -d '{"address": "669 Arlngton lace, Mac0n Georgia"}' | python3 -m json.tool
```

4. Save ALL raw JSON responses. For each address, note:
   - The `normalized_address` returned
   - Each provider's result (lat, lng, confidence, location_type, is_outlier)
   - The `cascade_trace` stages and their timings
   - Whether an `official` result was set (and by which provider/stage)
   - The `local_results` from fuzzy/spell stages

5. Generate GEOCODE-COMPARISON-REPORT.md with the following structure:

### Header
- Date, environment, API URL, providers active, cascade enabled status
- LLM sidecar status, spell dictionary status

### Test Addresses Table
| # | Raw Input | Input Defects |
(same 4 addresses with defect descriptions)

### Per-Address Sections (Address 1 through 4)
For each address:
1. **Normalized Address** — what the normalizer produced
2. **Cascade Trace** — table showing each stage (normalize, spell_correct, exact_match, fuzzy, llm_correct, consensus, auto_set) with: stage name, duration (ms), results count, early_exit flag, detail summary
3. **Provider Results** — table with columns: Provider, Confidence, Location Type, Lat, Lng, Is Outlier, Verdict
4. **Official Result** — which provider was selected as official, set_by stage, confidence
5. **Analysis** — 2-3 sentences on how the cascade handled this address's specific defects

### Comparison Matrix
Cross-address table showing:
| Address | Spell Corrected? | Fuzzy Matched? | LLM Corrected? | Official Set? | Official Provider | Confidence |

### Delta from Prior Run (260329-q2v)
Note any differences from the prior test run — bug fixes, new behavior, changed results.

### Findings
- Which cascade stages activated for which defect types
- How LLM correction handled address 4 (if LLM active)
- Provider agreement/disagreement patterns
- Any unexpected behavior or failures
  </action>
  <verify>
    <automated>test -f .planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md && grep -c "Address" .planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/GEOCODE-COMPARISON-REPORT.md</automated>
  </verify>
  <done>GEOCODE-COMPARISON-REPORT.md exists with all 4 addresses documented, cascade traces captured, provider results compared, delta from prior run noted, and findings section analyzing cascade behavior across progressively degraded inputs.</done>
</task>

</tasks>

<verification>
- GEOCODE-COMPARISON-REPORT.md contains results for all 4 test addresses
- Each address section includes cascade trace, provider results table, and official result
- Comparison matrix shows which cascade stages activated per address
- Delta section compares with prior run 260329-q2v
- Findings section analyzes cascade stage effectiveness by defect type
</verification>

<success_criteria>
- All 4 addresses geocoded with trace=true responses captured
- Report clearly shows the cascade pipeline behavior for each address
- Report demonstrates how progressively worse input quality triggers different cascade stages
- Official results documented with set_by source and confidence scores
</success_criteria>

<output>
After completion, create `.planning/quick/260329-qm8-test-geocoding-providers-and-ai-with-exa/260329-qm8-SUMMARY.md`
</output>
