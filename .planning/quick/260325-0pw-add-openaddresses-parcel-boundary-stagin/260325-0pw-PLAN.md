---
phase: quick
plan: 260325-0pw
type: execute
wave: 1
depends_on: []
files_modified:
  - src/civpulse_geo/models/parcels.py
  - src/civpulse_geo/models/__init__.py
  - alembic/versions/d4a71c3f8b92_add_oa_parcels_table.py
  - src/civpulse_geo/cli/__init__.py
autonomous: true
requirements: [QUICK-260325-0pw]

must_haves:
  truths:
    - "oa_parcels table exists in database after migration"
    - "load-oa-parcels CLI command is registered and callable"
    - "Existing tests still pass with no regressions"
  artifacts:
    - path: "src/civpulse_geo/models/parcels.py"
      provides: "OpenAddressesParcel ORM model with Geography POLYGON"
    - path: "alembic/versions/d4a71c3f8b92_add_oa_parcels_table.py"
      provides: "Migration creating oa_parcels table with GiST index"
    - path: "src/civpulse_geo/cli/__init__.py"
      provides: "load-oa-parcels command, _parse_oa_parcel_feature, _upsert_oa_parcel_batch"
  key_links:
    - from: "src/civpulse_geo/models/parcels.py"
      to: "src/civpulse_geo/models/__init__.py"
      via: "import and __all__ export"
      pattern: "from .parcels import OpenAddressesParcel"
---

<objective>
Add OpenAddresses parcel boundary staging table and load-oa-parcels CLI command.

Purpose: Store parcel boundary polygons from OpenAddresses data, linked to existing OA address points via FK, enabling future spatial queries for parcel lookup.
Output: ORM model, Alembic migration, and CLI bulk-load command.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify implementation completeness</name>
  <files>src/civpulse_geo/models/parcels.py, src/civpulse_geo/models/__init__.py, alembic/versions/d4a71c3f8b92_add_oa_parcels_table.py, src/civpulse_geo/cli/__init__.py</files>
  <action>Implementation is already complete. Verify the four artifacts exist and are well-formed:

1. Confirm src/civpulse_geo/models/parcels.py defines OpenAddressesParcel with Base, TimestampMixin, Geography POLYGON column, and FK to addresses.id
2. Confirm src/civpulse_geo/models/__init__.py imports and exports OpenAddressesParcel in __all__
3. Confirm alembic/versions/d4a71c3f8b92_add_oa_parcels_table.py creates oa_parcels table with GiST spatial index and lookup indexes
4. Confirm src/civpulse_geo/cli/__init__.py has _parse_oa_parcel_feature(), _upsert_oa_parcel_batch(), and load_oa_parcels command registered on the Typer app

If any artifact is missing or malformed, fix it. Otherwise proceed to verification.</action>
  <verify>
    <automated>python -c "from civpulse_geo.models.parcels import OpenAddressesParcel; print('model OK')" && python -c "from civpulse_geo.models import OpenAddressesParcel; print('init OK')" && python -m civpulse_geo.cli --help | grep load-oa-parcels && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>All four files exist, model imports cleanly, CLI command is registered</done>
</task>

<task type="auto">
  <name>Task 2: Run existing test suite for regression check</name>
  <files></files>
  <action>Run the full pytest suite to confirm no regressions from the new model, migration, or CLI additions. No new test files are expected for this quick task — the goal is to confirm nothing broke.

Run: pytest tests/ -x -q

If any test fails, diagnose and fix the regression.</action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civpulse/geo-api && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All existing tests pass with zero failures</done>
</task>

</tasks>

<verification>
- OpenAddressesParcel model importable from both parcels module and models __init__
- load-oa-parcels appears in CLI --help output
- All existing tests pass
</verification>

<success_criteria>
- Model, migration, and CLI command are verified as complete and functional
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260325-0pw-add-openaddresses-parcel-boundary-stagin/260325-0pw-SUMMARY.md`
</output>
