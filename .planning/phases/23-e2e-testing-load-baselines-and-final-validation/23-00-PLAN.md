---
phase: 23-e2e-testing-load-baselines-and-final-validation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "ArgoCD Application CRs for geo-api-dev and geo-api-prod are applied and show Synced/Healthy"
    - "K8s Secret geo-api-secret exists in civpulse-prod with correct DATABASE_URL and DATABASE_URL_SYNC"
    - "geo-api pod in civpulse-prod reaches Running state with all containers ready"
    - "Port-forward to geo-api in civpulse-prod succeeds and /health/ready returns 200"
  artifacts: []
  key_links:
    - from: "k8s/overlays/prod/argocd-app.yaml"
      to: "ArgoCD in argocd namespace"
      via: "kubectl apply"
      pattern: "kubectl apply -f"
    - from: "geo-api-secret"
      to: "geo-api Deployment"
      via: "secretRef in deployment.yaml envFrom"
      pattern: "geo-api-secret"
---

<objective>
Deploy geo-api to the civpulse-prod K8s namespace so that E2E tests, load tests, and observability verification can run against a live service.

Purpose: The geo-api pod does NOT currently exist in civpulse-prod. ArgoCD Application CRs have never been applied, and the K8s Secret geo-api-secret has not been created. Without a running pod, all subsequent plans (E2E tests, load tests, verification scripts) will fail with connection refused. This plan establishes the deployment prerequisite.

Output: A running geo-api pod in civpulse-prod that responds to /health/ready with 200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-CONTEXT.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-RESEARCH.md

<interfaces>
<!-- K8s manifests and credentials needed for deployment -->

From k8s/overlays/prod/argocd-app.yaml:
  - ArgoCD Application CR: geo-api-prod targeting k8s/overlays/prod on main branch
  - Destination namespace: civpulse-prod
  - syncPolicy: automated with prune and selfHeal

From k8s/overlays/dev/argocd-app.yaml:
  - ArgoCD Application CR: geo-api-dev targeting k8s/overlays/dev on main branch
  - Destination namespace: civpulse-dev

From k8s/base/deployment.yaml:
  - Secret ref: geo-api-secret (envFrom secretRef)
  - Expected env vars in secret: DATABASE_URL, DATABASE_URL_SYNC
  - Init containers: db-wait, alembic-migrate, spell-rebuild, model-pull, ollama (sidecar)
  - Main container: geo-api on port 8000
  - Probes: /health/live (liveness), /health/ready (readiness)

From src/civpulse_geo/config.py:
  - DATABASE_URL format: postgresql+asyncpg://{user}:{pass}@{host}:5432/{db}
  - DATABASE_URL_SYNC format: postgresql+psycopg2://{user}:{pass}@{host}:5432/{db}

DB credentials (from project memory):
  - geo_prod user: civpulse_geo_prod
  - DB host: postgresql.civpulse-infra.svc.cluster.local
  - DB name: civpulse_geo_prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create K8s Secret and apply ArgoCD Application CRs</name>
  <files></files>
  <action>
    This task applies cluster resources — no repo files are created or modified.

    1. **Check if geo-api-secret already exists in civpulse-prod:**
       ```
       kubectl get secret geo-api-secret -n civpulse-prod
       ```
       If it exists, skip to step 3.

    2. **Create K8s Secret in civpulse-prod** (if missing):
       The Secret must contain DATABASE_URL and DATABASE_URL_SYNC.

       Use the credentials from project memory (project_db_credentials.md):
       - User: civpulse_geo_prod
       - Password: L7mYpqUwn6BVx7X8R6xhWJxsGDF6x3S
       - Host: postgresql.civpulse-infra.svc.cluster.local
       - Port: 5432
       - Database: civpulse_geo_prod

       ```
       kubectl create secret generic geo-api-secret -n civpulse-prod \
         --from-literal=DATABASE_URL="postgresql+asyncpg://civpulse_geo_prod:L7mYpqUwn6BVx7X8R6xhWJxsGDF6x3S@postgresql.civpulse-infra.svc.cluster.local:5432/civpulse_geo_prod" \
         --from-literal=DATABASE_URL_SYNC="postgresql+psycopg2://civpulse_geo_prod:L7mYpqUwn6BVx7X8R6xhWJxsGDF6x3S@postgresql.civpulse-infra.svc.cluster.local:5432/civpulse_geo_prod"
       ```

    3. **Check if ArgoCD apps already exist:**
       ```
       kubectl get applications -n argocd | grep geo-api
       ```
       If both geo-api-dev and geo-api-prod exist, skip to step 5.

    4. **Apply ArgoCD Application CRs:**
       ```
       kubectl apply -f k8s/overlays/dev/argocd-app.yaml
       kubectl apply -f k8s/overlays/prod/argocd-app.yaml
       ```

    5. **Wait for ArgoCD to sync and create resources.** ArgoCD has automated sync with selfHeal enabled, so it will create the Deployment, Service, ConfigMap, and PVC automatically. Allow up to 5 minutes for sync + pod startup (init containers: db-wait, alembic-migrate, spell-rebuild, model-pull take time).

    6. **Also create geo-api-secret in civpulse-dev** (if missing):
       ```
       kubectl get secret geo-api-secret -n civpulse-dev || \
       kubectl create secret generic geo-api-secret -n civpulse-dev \
         --from-literal=DATABASE_URL="postgresql+asyncpg://civpulse_geo_dev:k0eiA3J1WvU9UmJiYbGfLzVtpTQCye@postgresql.civpulse-infra.svc.cluster.local:5432/civpulse_geo_dev" \
         --from-literal=DATABASE_URL_SYNC="postgresql+psycopg2://civpulse_geo_dev:k0eiA3J1WvU9UmJiYbGfLzVtpTQCye@postgresql.civpulse-infra.svc.cluster.local:5432/civpulse_geo_dev"
       ```
  </action>
  <verify>
    <automated>kubectl get secret geo-api-secret -n civpulse-prod -o jsonpath='{.metadata.name}' && echo " OK" && kubectl get applications -n argocd geo-api-prod -o jsonpath='{.status.sync.status}' && echo " sync" && kubectl get applications -n argocd geo-api-dev -o jsonpath='{.status.sync.status}' && echo " sync"</automated>
  </verify>
  <done>K8s Secret geo-api-secret exists in civpulse-prod (and civpulse-dev). ArgoCD Applications geo-api-prod and geo-api-dev exist and show Synced status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify geo-api pod is Running and healthy</name>
  <what-built>ArgoCD Application CRs applied, K8s Secrets created, and ArgoCD sync triggered for both dev and prod environments. The pod should now be starting up with init containers (db-wait, alembic-migrate, spell-rebuild, model-pull) followed by the geo-api main container and Ollama sidecar.</what-built>
  <how-to-verify>
    1. Check pod status (may need to wait 3-5 minutes for init containers):
       ```
       kubectl get pods -n civpulse-prod -l app.kubernetes.io/name=geo-api -w
       ```
       Expected: Pod shows Running with all containers ready (e.g., 2/2).

    2. Check ArgoCD app health:
       ```
       kubectl get applications -n argocd geo-api-prod -o jsonpath='{.status.health.status}'
       ```
       Expected: "Healthy"

    3. Start port-forward and test health:
       ```
       kubectl port-forward -n civpulse-prod svc/geo-api 8000:8000 &
       sleep 2
       curl -s http://localhost:8000/health/ready
       ```
       Expected: HTTP 200 with provider registration info.

    4. If pod is in CrashLoopBackOff or init containers are failing, check logs:
       ```
       kubectl logs -n civpulse-prod -l app.kubernetes.io/name=geo-api --all-containers --tail=50
       ```

    5. Kill the port-forward when done checking:
       ```
       kill %1
       ```
  </how-to-verify>
  <resume-signal>Type "approved" if pod is Running and /health/ready returns 200, or describe the issue if something is failing.</resume-signal>
</task>

</tasks>

<verification>
- `kubectl get pods -n civpulse-prod -l app.kubernetes.io/name=geo-api -o jsonpath='{.items[0].status.phase}'` returns "Running"
- `kubectl get applications -n argocd | grep geo-api` shows both dev and prod as Synced/Healthy
- `kubectl get secret geo-api-secret -n civpulse-prod` succeeds
- Port-forward to svc/geo-api on port 8000 succeeds and `/health/ready` returns 200
</verification>

<success_criteria>
- geo-api pod is Running in civpulse-prod with all containers ready
- ArgoCD shows geo-api-prod as Synced and Healthy
- /health/ready returns 200 via port-forward (confirming DB connectivity and provider registration)
- K8s Secret with DATABASE_URL credentials exists in both namespaces
</success_criteria>

<output>
After completion, create `.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-00-SUMMARY.md`
</output>
