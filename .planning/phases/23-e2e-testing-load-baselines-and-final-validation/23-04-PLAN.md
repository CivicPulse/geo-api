---
phase: 23-e2e-testing-load-baselines-and-final-validation
plan: 04
type: execute
wave: 2
depends_on: [23-01, 23-02, 23-03]
files_modified:
  - .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "A structured validation checklist exists covering all 7 categories: debt, review, observability, deployment, resilience, testing, validation"
    - "Each checklist item has a specific verification command or check that can be executed"
    - "The checklist has a blockers section and a non-blockers section for triage"
    - "The checklist is designed for iterative pass-until-clean execution per D-14"
  artifacts:
    - path: ".planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md"
      provides: "7-category production readiness validation checklist"
      contains: "## 1. Tech Debt"
  key_links:
    - from: "23-VALIDATION-CHECKLIST.md"
      to: "tests/e2e/"
      via: "References E2E test commands"
      pattern: "pytest tests/e2e/"
    - from: "23-VALIDATION-CHECKLIST.md"
      to: "loadtests/"
      via: "References Locust load test commands"
      pattern: "locust -f"
    - from: "23-VALIDATION-CHECKLIST.md"
      to: "scripts/verify/"
      via: "References verification script commands"
      pattern: "scripts/verify/"
---

<objective>
Create the final top-to-bottom production readiness validation checklist covering all 7 milestone categories.

Purpose: Provides a structured, repeatable checklist that is run iteratively until all items pass. Blockers found during the pass are fixed in-phase (VAL-01), non-blockers are logged for subsequent phases (VAL-02), and the full pass repeats until clean (VAL-03). This is the final gate before declaring v1.3 production-ready.

Output: `23-VALIDATION-CHECKLIST.md` with 7 category sections, specific verification commands, pass/fail status tracking, and blocker/non-blocker triage sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-CONTEXT.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7-category validation checklist</name>
  <files>
    .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-CONTEXT.md
    pyproject.toml
    k8s/base/deployment.yaml
    k8s/base/service.yaml
    src/civpulse_geo/observability/metrics.py
    src/civpulse_geo/observability/tracing.py
    src/civpulse_geo/observability/logging.py
    src/civpulse_geo/api/health.py
  </read_first>
  <action>
    Create `.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md` (per D-13, D-14, D-15, D-16) with the following exact structure:

    ```markdown
    # Phase 23: Final Validation Checklist

    **Created:** YYYY-MM-DD
    **Last run:** _not yet_
    **Status:** Not started
    **Pass count:** 0

    ## Instructions

    Run each category top-to-bottom. Record PASS/FAIL with date.
    - **FAIL (blocker):** Fix in-phase per VAL-01, then re-run FULL checklist from category 1.
    - **FAIL (non-blocker):** Log in Non-Blockers section per VAL-02, continue.
    - **Complete when:** All items show PASS with no open blockers (VAL-03).

    ---

    ## 1. Tech Debt (DEBT-01 through DEBT-04)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 1.1 | Tiger provider timeout resolved | `uv run pytest tests/test_tiger_provider.py -x` passes | | |
    | 1.2 | Cache hit works on repeated calls | `uv run pytest tests/test_geocoding_service.py -k cache -x` passes | | |
    | 1.3 | Spell dictionary auto-populates | `uv run pytest tests/test_spell_startup.py -x` passes | | |
    | 1.4 | CLI tests pass | `uv run pytest tests/test_import_cli.py tests/test_load_oa_cli.py -x` passes | | |
    | 1.5 | Full test suite passes | `uv run pytest tests/ --ignore=tests/e2e -x` passes (all non-E2E tests) | | |

    ## 2. Code Review (REVIEW-01 through REVIEW-03)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 2.1 | No hardcoded credentials in source | `grep -rn "CHANGEME" src/ --include="*.py"` returns only config.py defaults | | |
    | 2.2 | Input validation on geocode endpoint | `grep "max_length=500" src/civpulse_geo/schemas/geocoding.py` shows constraint | | |
    | 2.3 | Provider name allowlist exists | `grep "KNOWN_PROVIDERS" src/civpulse_geo/api/geocoding.py` shows frozenset | | |
    | 2.4 | Global exception handler present | `grep "exception_handler" src/civpulse_geo/main.py` shows registration | | |
    | 2.5 | Pool sizing configured | `grep "db_pool_size" src/civpulse_geo/config.py` shows explicit setting | | |

    ## 3. Observability (OBS-01 through OBS-04)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 3.1 | Structured JSON logging configured | `grep "serialize=True" src/civpulse_geo/observability/logging.py` | | |
    | 3.2 | Prometheus /metrics endpoint exists | `grep "generate_latest" src/civpulse_geo/api/metrics.py` | | |
    | 3.3 | OTel tracing module configured | `grep "civpulse-geo" src/civpulse_geo/observability/tracing.py` shows service name | | |
    | 3.4 | Request ID middleware exists | `grep "RequestIDMiddleware" src/civpulse_geo/observability/middleware.py` | | |
    | 3.5 | OTel patcher for trace context | `grep "trace_id" src/civpulse_geo/observability/middleware.py` shows injection | | |
    | 3.6 | Metrics instrumentation | `uv run pytest tests/test_metrics_instrumentation.py -x` passes | | |
    | 3.7 | Tracing tests pass | `uv run pytest tests/test_tracing.py -x` passes | | |
    | 3.8 | Loki logs verified under load | `uv run python scripts/verify/verify_loki.py` exits 0 | | |
    | 3.9 | Tempo traces verified under load | `uv run python scripts/verify/verify_tempo.py` exits 0 | | |
    | 3.10 | VictoriaMetrics metrics verified | `uv run python scripts/verify/verify_victoriametrics.py` exits 0 | | |

    ## 4. Deployment (DEPLOY-01 through DEPLOY-08)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 4.1 | Dockerfile exists and builds | `docker build -t geo-api:test .` succeeds | | |
    | 4.2 | Dockerfile uses non-root user | `grep "appuser" Dockerfile` shows USER directive | | |
    | 4.3 | K8s deployment manifest exists | `test -f k8s/base/deployment.yaml` | | |
    | 4.4 | K8s service manifest exists | `test -f k8s/base/service.yaml` | | |
    | 4.5 | Dev overlay exists | `test -f k8s/overlays/dev/kustomization.yaml` | | |
    | 4.6 | Prod overlay exists | `test -f k8s/overlays/prod/kustomization.yaml` | | |
    | 4.7 | ArgoCD app CRs exist | `test -f k8s/overlays/dev/argocd-app.yaml && test -f k8s/overlays/prod/argocd-app.yaml` | | |
    | 4.8 | Pod running in prod | `kubectl get pods -n civpulse-prod -l app.kubernetes.io/name=geo-api -o jsonpath='{.items[0].status.phase}'` returns Running | | |
    | 4.9 | DB reachable from pod | `kubectl exec -n civpulse-prod deploy/geo-api -- python -c "import asyncio; print('DB OK')"` or health/ready returns 200 | | |

    ## 5. Resilience (RESIL-01 through RESIL-04)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 5.1 | Liveness probe works | `kubectl port-forward -n civpulse-prod svc/geo-api 8000:8000 & curl -s http://localhost:8000/health/live` returns 200 | | |
    | 5.2 | Readiness probe works | `curl -s http://localhost:8000/health/ready` returns 200 (via port-forward) | | |
    | 5.3 | Graceful shutdown tested | `uv run pytest tests/test_shutdown.py -x` passes | | |
    | 5.4 | Health tests pass | `uv run pytest tests/test_health.py -x` passes | | |
    | 5.5 | Startup init containers succeed | `kubectl get pods -n civpulse-prod -l app.kubernetes.io/name=geo-api -o jsonpath='{.items[0].status.initContainerStatuses[*].ready}'` shows all true | | |

    ## 6. Testing (TEST-01 through TEST-06)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 6.1 | E2E geocode tests pass all 5 providers | `GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/test_providers_geocode.py -v -m e2e` (with port-forward active) | | |
    | 6.2 | E2E validate tests pass all 4 providers | `GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/test_providers_validate.py -v -m e2e` (with port-forward active) | | |
    | 6.3 | E2E cascade pipeline test passes | `GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/test_cascade_pipeline.py -v -m e2e` (with port-forward active) | | |
    | 6.4 | Cold-cache Locust baseline captured | `locust -f loadtests/geo_api_locustfile.py --headless --users 30 --spawn-rate 0.25 --run-time 7m --host http://localhost:8000 --csv loadtests/reports/cold_cache --csv-full-history` completes with CSV output | | |
    | 6.5 | Warm-cache Locust baseline captured | `GEO_LOADTEST_WARM=1 locust -f loadtests/geo_api_locustfile.py --headless --users 30 --spawn-rate 0.25 --run-time 7m --host http://localhost:8000 --csv loadtests/reports/warm_cache --csv-full-history` completes with CSV output | | |
    | 6.6 | Cold-cache P95 below 10s | Review `loadtests/reports/cold_cache_stats.csv` — Aggregated row P95 < 10000ms | | |
    | 6.7 | Warm-cache P95 below 2s | Review `loadtests/reports/warm_cache_stats.csv` — Aggregated row P95 < 2000ms | | |

    ## 7. Validation Process (VAL-01 through VAL-03)

    | # | Check | Command / Verification | Status | Date |
    |---|-------|------------------------|--------|------|
    | 7.1 | All blockers resolved in-phase | No items in Blockers section below (or all resolved) | | |
    | 7.2 | Non-blockers logged | All non-blocker items documented in Non-Blockers section below | | |
    | 7.3 | Full pass clean | All items in categories 1-6 show PASS | | |

    ---

    ## Blockers Found

    _Items that must be fixed before the validation pass can be marked clean._

    | # | Category | Item | Description | Resolution | Date Fixed |
    |---|----------|------|-------------|------------|------------|
    | | | | _none yet_ | | |

    ## Non-Blockers Logged

    _Items deferred to subsequent phases or future milestones per VAL-02._

    | # | Category | Item | Description | Severity | Deferred To |
    |---|----------|------|-------------|----------|-------------|
    | | | | _none yet_ | | |

    ---

    ## Run Log

    | Run # | Date | Result | Notes |
    |-------|------|--------|-------|
    | | | | _not yet run_ |
    ```

    The date placeholder `YYYY-MM-DD` should be replaced with the actual current date at creation time.

    Key design decisions:
    - Each check has a specific command that can be copy-pasted and executed.
    - Status column is filled in during execution (PASS/FAIL).
    - Latency thresholds (6.6, 6.7) are intentionally generous for initial baselines — cold-cache 10s and warm-cache 2s at P95 are starting points. The actual baselines will be recorded in the run log.
    - The Run Log at the bottom tracks each full pass attempt for audit trail (D-16).
  </action>
  <verify>
    <automated>test -f .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md && grep -c "## [0-9]" .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md | xargs test 7 -eq && grep -q "Blockers Found" .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md && grep -q "Non-Blockers" .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 23-VALIDATION-CHECKLIST.md exists in the phase directory
    - File contains exactly 7 numbered category headers (## 1 through ## 7)
    - Category 1 header contains "Tech Debt"
    - Category 2 header contains "Code Review"
    - Category 3 header contains "Observability"
    - Category 4 header contains "Deployment"
    - Category 5 header contains "Resilience"
    - Category 6 header contains "Testing"
    - Category 7 header contains "Validation Process"
    - File contains "## Blockers Found" section
    - File contains "## Non-Blockers Logged" section
    - File contains "## Run Log" section
    - Each checklist item row has a Command / Verification column with a specific command
    - File references `pytest tests/e2e/` for E2E test checks
    - File references `locust -f loadtests/` for load test checks
    - File references `scripts/verify/` for observability verification checks
  </acceptance_criteria>
  <done>Validation checklist covers all 7 categories with specific verification commands, blocker/non-blocker triage sections, and a run log for iterative pass-until-clean execution. Total of 30+ individual check items spanning debt, review, observability, deployment, resilience, testing, and validation process.</done>
</task>

</tasks>

<verification>
- `grep -c "| [0-9]" .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-VALIDATION-CHECKLIST.md` shows 30+ checklist items
- All 7 categories present and cover the full v1.3 requirement set
- Blocker and non-blocker sections exist for triage
- Run log section exists for audit trail
</verification>

<success_criteria>
- 7-category validation checklist exists with 30+ specific check items
- Each item has a concrete verification command
- Blockers section, non-blockers section, and run log present
- Checklist covers all v1.3 requirement categories (debt, review, observability, deployment, resilience, testing, validation)
- Designed for iterative execution until clean per VAL-03
</success_criteria>

<output>
After completion, create `.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-04-SUMMARY.md`
</output>
