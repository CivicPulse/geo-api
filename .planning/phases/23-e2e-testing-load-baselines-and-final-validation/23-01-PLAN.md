---
phase: 23-e2e-testing-load-baselines-and-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/__init__.py
  - tests/e2e/conftest.py
  - tests/e2e/fixtures/provider_addresses.yaml
  - tests/e2e/test_providers_geocode.py
  - tests/e2e/test_providers_validate.py
  - tests/e2e/test_cascade_pipeline.py
  - pyproject.toml
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "E2E geocode tests pass for all 5 providers (census, openaddresses, postgis_tiger, national_address_database, macon_bibb) against deployed service"
    - "E2E validate tests pass for all 4 validation providers (openaddresses, postgis_tiger, national_address_database, macon_bibb) against deployed service"
    - "E2E cascade pipeline test resolves a degraded/misspelled address to a correct geocode with cascade_trace populated"
    - "pytest marker 'e2e' is registered in pyproject.toml and selectable via -m e2e"
  artifacts:
    - path: "tests/e2e/conftest.py"
      provides: "E2E fixtures: e2e_client (real httpx.AsyncClient), provider_addresses (YAML loader)"
      contains: "httpx.AsyncClient"
    - path: "tests/e2e/fixtures/provider_addresses.yaml"
      provides: "Per-provider known-good Macon-Bibb test addresses"
      contains: "census"
    - path: "tests/e2e/test_providers_geocode.py"
      provides: "Parametrized geocode E2E tests for all 5 providers"
      contains: "pytest.mark.e2e"
    - path: "tests/e2e/test_providers_validate.py"
      provides: "Parametrized validate E2E tests for 4 validation providers"
      contains: "pytest.mark.e2e"
    - path: "tests/e2e/test_cascade_pipeline.py"
      provides: "Cascade pipeline E2E test with degraded input"
      contains: "cascade_trace"
  key_links:
    - from: "tests/e2e/conftest.py"
      to: "GEO_API_BASE_URL env var"
      via: "os.environ.get with http://localhost:8000 default"
      pattern: "GEO_API_BASE_URL"
    - from: "tests/e2e/test_providers_geocode.py"
      to: "tests/e2e/fixtures/provider_addresses.yaml"
      via: "provider_addresses fixture"
      pattern: "provider_addresses"
    - from: "tests/e2e/test_cascade_pipeline.py"
      to: "/geocode endpoint"
      via: "e2e_client.post with trace=true"
      pattern: "cascade_trace"
---

<objective>
Create the E2E test suite for all 5 geocoding providers and the cascade pipeline against a deployed geo-api service.

Purpose: Validates that every provider (Census, OpenAddresses, Tiger, NAD, Macon-Bibb) returns correct geocode and validate results when hit via real HTTP against the deployed prod service. Also validates the full cascade pipeline resolves degraded input end-to-end. This is the core correctness gate for production readiness.

Output: `tests/e2e/` directory with conftest, fixture data, and 3 test modules; updated pyproject.toml with `e2e` marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-CONTEXT.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/civpulse_geo/api/geocoding.py:
- Router prefix: /geocode
- POST /geocode — accepts GeocodeRequest, returns GeocodeResponse
- POST /geocode/batch — accepts BatchGeocodeRequest

From src/civpulse_geo/api/validation.py:
- Router prefix: /validate
- POST /validate — accepts ValidateRequest, returns ValidateResponse
- POST /validate/batch — accepts BatchValidateRequest

From src/civpulse_geo/schemas/geocoding.py:
```python
class GeocodeRequest(BaseModel):
    address: str = Field(..., min_length=1, max_length=500)

class GeocodeResponse(BaseModel):
    address_hash: str
    # ...
    results: list[GeocodeProviderResult]
    local_results: list[GeocodeProviderResult] = []
    official: GeocodeProviderResult | None = None
    cascade_trace: list[dict] | None = None
    would_set_official: GeocodeProviderResult | None = None
```

Provider names (from main.py registration): "census", "openaddresses", "postgis_tiger", "national_address_database", "macon_bibb"

Validation providers: "openaddresses", "postgis_tiger", "national_address_database", "macon_bibb" (Census does not have a validation provider)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test infrastructure and provider fixtures</name>
  <files>
    tests/e2e/__init__.py
    tests/e2e/conftest.py
    tests/e2e/fixtures/provider_addresses.yaml
    pyproject.toml
  </files>
  <read_first>
    pyproject.toml
    tests/conftest.py
    src/civpulse_geo/api/geocoding.py
    src/civpulse_geo/api/validation.py
    src/civpulse_geo/schemas/geocoding.py
    src/civpulse_geo/schemas/validation.py
  </read_first>
  <action>
    1. Create `tests/e2e/__init__.py` (empty file).

    2. Create `tests/e2e/conftest.py` with two fixtures (per D-02, D-03):
       - `e2e_client`: function-scoped async fixture creating `httpx.AsyncClient(base_url=os.environ.get("GEO_API_BASE_URL", "http://localhost:8000"), timeout=30.0)` using async context manager. Do NOT import from `tests/conftest.py` — E2E conftest is fully independent (anti-pattern from research).
       - `provider_addresses`: session-scoped sync fixture that loads `tests/e2e/fixtures/provider_addresses.yaml` via `yaml.safe_load()`. Requires `pyyaml` — add as dev dependency with `uv add --dev pyyaml`.
       - Import `os`, `pytest`, `httpx`, `yaml`.

    3. Create `tests/e2e/fixtures/provider_addresses.yaml` (per D-04) with this exact structure:
       ```yaml
       census:
         geocode_address: "456 First St, Macon, GA 31201"
         expected_provider: "census"
       openaddresses:
         geocode_address: "2720 Riverside Dr, Macon, GA 31204"
         validate_address: "2720 Riverside Dr, Macon, GA 31204"
         expected_provider: "openaddresses"
       postgis_tiger:
         geocode_address: "3261 Vineville Ave, Macon, GA 31204"
         validate_address: "3261 Vineville Ave, Macon, GA 31204"
         expected_provider: "postgis_tiger"
       national_address_database:
         geocode_address: "1234 Forsyth St, Macon, GA 31201"
         validate_address: "1234 Forsyth St, Macon, GA 31201"
         expected_provider: "national_address_database"
       macon_bibb:
         geocode_address: "700 Poplar St, Macon, GA 31201"
         validate_address: "700 Poplar St, Macon, GA 31201"
         expected_provider: "macon_bibb"
       cascade_test:
         degraded_address: "123 Coliseum Dr, Mcon, GA 31201"
         note: "Misspelled city triggers spell-correct + cascade"
       ```
       These are real Macon-Bibb County addresses (Bibb County jurisdiction where GIS data is loaded). The specific addresses may need adjustment if they don't resolve — the E2E tests themselves will reveal this.

    4. Update `pyproject.toml` — add the `e2e` marker to the existing `markers` list (per Pitfall 2 from research):
       ```
       markers = [
           "tiger: marks tests that require Tiger/Line data loaded (skipped otherwise)",
           "e2e: marks tests as end-to-end requiring a deployed service",
       ]
       ```
       Also add `pyyaml` to dev dependencies if not already present: run `uv add --dev pyyaml`.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; print('pyyaml OK')" && grep -q "e2e:" pyproject.toml && test -f tests/e2e/conftest.py && test -f tests/e2e/fixtures/provider_addresses.yaml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the line: `"e2e: marks tests as end-to-end requiring a deployed service"`
    - tests/e2e/__init__.py exists (can be empty)
    - tests/e2e/conftest.py contains `GEO_API_BASE_URL` and `httpx.AsyncClient`
    - tests/e2e/conftest.py contains `provider_addresses` fixture with `yaml.safe_load`
    - tests/e2e/conftest.py does NOT import from `tests/conftest`
    - tests/e2e/fixtures/provider_addresses.yaml contains keys: census, openaddresses, postgis_tiger, national_address_database, macon_bibb, cascade_test
    - pyyaml is in dev dependencies in pyproject.toml
  </acceptance_criteria>
  <done>E2E test infrastructure exists: conftest with real HTTP client fixture, YAML address fixtures for all 5 providers, e2e marker registered in pyproject.toml, pyyaml dev dependency installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create provider E2E tests and cascade pipeline test</name>
  <files>
    tests/e2e/test_providers_geocode.py
    tests/e2e/test_providers_validate.py
    tests/e2e/test_cascade_pipeline.py
  </files>
  <read_first>
    tests/e2e/conftest.py
    tests/e2e/fixtures/provider_addresses.yaml
    src/civpulse_geo/schemas/geocoding.py
    src/civpulse_geo/schemas/validation.py
    src/civpulse_geo/api/geocoding.py
    src/civpulse_geo/api/validation.py
  </read_first>
  <action>
    1. Create `tests/e2e/test_providers_geocode.py` (per D-01, D-05, TEST-01):
       - Set `pytestmark = pytest.mark.e2e` at module level.
       - Parametrize over all 5 provider names: `["census", "openaddresses", "postgis_tiger", "national_address_database", "macon_bibb"]`.
       - `test_geocode_provider(e2e_client, provider_addresses, provider_name)`:
         - Get `address = provider_addresses[provider_name]["geocode_address"]`
         - POST to `/geocode` with JSON body `{"address": address}`
         - Assert `resp.status_code == 200`
         - Parse `body = resp.json()`
         - Assert `body["address_hash"]` is a non-empty string
         - Collect all provider names from `body["results"] + body.get("local_results", [])` via `[r["provider_name"] for r in all_results]`
         - Assert `provider_name in providers`, with descriptive failure message showing which providers were returned
       - Add a `test_geocode_returns_coordinates(e2e_client, provider_addresses)` test:
         - POST to `/geocode` with the census address
         - Assert at least one result has `latitude` and `longitude` that are non-None float values

    2. Create `tests/e2e/test_providers_validate.py` (per D-05, TEST-01):
       - Set `pytestmark = pytest.mark.e2e` at module level.
       - Parametrize over the 4 validation providers: `["openaddresses", "postgis_tiger", "national_address_database", "macon_bibb"]` (Census has no validation provider).
       - `test_validate_provider(e2e_client, provider_addresses, provider_name)`:
         - Get `address = provider_addresses[provider_name]["validate_address"]`
         - POST to `/validate` with JSON body `{"address": address}`
         - Assert `resp.status_code == 200`
         - Parse `body = resp.json()`
         - Assert `body["candidates"]` is a non-empty list
         - Collect provider names from candidates: `[c["provider_name"] for c in body["candidates"]]`
         - Assert `provider_name in candidate_providers`

    3. Create `tests/e2e/test_cascade_pipeline.py` (per D-05, TEST-02):
       - Set `pytestmark = pytest.mark.e2e` at module level.
       - `test_cascade_resolves_degraded_input(e2e_client, provider_addresses)`:
         - Get degraded address from `provider_addresses["cascade_test"]["degraded_address"]`
         - POST to `/geocode` with JSON body `{"address": degraded_address}` and query params `trace=true`
         - Assert `resp.status_code == 200`
         - Parse `body = resp.json()`
         - Assert `body["cascade_trace"]` is not None and is a non-empty list
         - Extract stage names: `[s["stage"] for s in body["cascade_trace"]]`
         - Assert at least one stage ran (len > 0)
         - Assert `body["results"]` or `body["local_results"]` is non-empty (at least one provider returned a result)
       - `test_cascade_dry_run_shows_would_set_official(e2e_client, provider_addresses)`:
         - Use a known-good address (e.g., census geocode_address)
         - POST to `/geocode` with JSON body and query params `trace=true&dry_run=true`
         - Assert `resp.status_code == 200`
         - Parse body and assert `body.get("would_set_official")` is not None OR `body.get("official")` is not None (the cascade either dry-ran and showed what it would set, or an official already exists)

    All test functions are `async def` to work with pytest-asyncio `asyncio_mode = "auto"`.
  </action>
  <verify>
    <automated>uv run python -m py_compile tests/e2e/test_providers_geocode.py && uv run python -m py_compile tests/e2e/test_providers_validate.py && uv run python -m py_compile tests/e2e/test_cascade_pipeline.py && uv run pytest --collect-only tests/e2e/ -m e2e 2>&1 | grep "test session starts" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/test_providers_geocode.py contains `pytestmark = pytest.mark.e2e`
    - tests/e2e/test_providers_geocode.py contains `@pytest.mark.parametrize` with all 5 provider names
    - tests/e2e/test_providers_geocode.py contains `e2e_client.post("/geocode"`
    - tests/e2e/test_providers_validate.py contains `pytestmark = pytest.mark.e2e`
    - tests/e2e/test_providers_validate.py contains `@pytest.mark.parametrize` with 4 validation provider names (no census)
    - tests/e2e/test_providers_validate.py contains `e2e_client.post("/validate"`
    - tests/e2e/test_cascade_pipeline.py contains `cascade_trace`
    - tests/e2e/test_cascade_pipeline.py contains `trace=true` or `"trace": "true"` in request params
    - All three files compile without syntax errors via `py_compile`
    - `uv run pytest --collect-only tests/e2e/ -m e2e` discovers at least 11 test items (5 geocode + 4 validate + 2 cascade)
  </acceptance_criteria>
  <done>E2E test suite has 11+ tests covering all 5 providers (geocode), 4 validation providers (validate), and 2 cascade pipeline scenarios. All tests use real HTTP via e2e_client fixture. Tests compile and are discoverable by pytest with the e2e marker.</done>
</task>

</tasks>

<verification>
- `uv run pytest --collect-only tests/e2e/ -m e2e` discovers 11+ tests
- `grep -c "pytest.mark.e2e" tests/e2e/test_*.py` shows all 3 files have the marker
- `grep "e2e:" pyproject.toml` shows the marker is registered
- All test files compile without errors
- NOTE: Actually running the tests requires `kubectl port-forward` to a deployed service. The tests themselves are verified for correctness by collection and compilation. Functional execution is validated during the Phase 23 validation pass.
</verification>

<success_criteria>
- 3 E2E test modules exist in tests/e2e/ with a total of 11+ test cases
- conftest.py provides real HTTP client fixture (not ASGITransport)
- Provider address fixture file covers all 5 providers plus cascade test data
- e2e pytest marker registered in pyproject.toml
- All files compile cleanly and tests are discoverable
</success_criteria>

<output>
After completion, create `.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-01-SUMMARY.md`
</output>
