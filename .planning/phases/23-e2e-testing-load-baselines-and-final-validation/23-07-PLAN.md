---
phase: 23-e2e-testing-load-baselines-and-final-validation
plan: 07
type: execute
wave: 2
depends_on: ["23-05", "23-06"]
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
gap_closure: true

must_haves:
  truths:
    - "E2E tests pass against deployed prod with all registered providers returning correct results"
    - "Locust cold-cache and warm-cache baseline reports are generated with P50/P95/P99 latencies"
    - "Observability verification scripts pass (or are triaged per Tempo decision in Plan 06)"
  artifacts: []
  key_links:
    - from: "tests/e2e/"
      to: "deployed geo-api prod service"
      via: "kubectl port-forward + httpx"
      pattern: "pytest -m e2e"
    - from: "loadtests/geo_api_locustfile.py"
      to: "deployed geo-api prod service"
      via: "kubectl port-forward + locust --headless"
      pattern: "locust --headless"
    - from: "scripts/verify/"
      to: "Loki, Tempo, VictoriaMetrics"
      via: "kubectl port-forward + httpx queries"
      pattern: "verify_loki|verify_tempo|verify_victoriametrics"
---

<objective>
Run the existing E2E tests, load tests, and observability verification scripts against the remediated prod environment.

Purpose: Plans 23-01 through 23-03 created all the test and verification assets. Plans 23-05 and 23-06 remediated the environment (loaded data, fixed Tempo/Tiger). This plan executes the existing test suites against the live environment to confirm the gaps are actually closed. No new code is written -- this is pure execution of existing assets.

Output: E2E test results, Locust baseline CSV reports, observability verification results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-01-SUMMARY.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-02-SUMMARY.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-03-SUMMARY.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-05-SUMMARY.md
@.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-06-SUMMARY.md

<interfaces>
<!-- E2E test suite (from 23-01-SUMMARY) -->
Tests in tests/e2e/:
  - test_providers_geocode.py — parametrized geocode tests per provider
  - test_providers_validate.py — parametrized validate tests per provider
  - test_cascade_pipeline.py — full cascade E2E test
  - conftest.py — base_url fixture reading GEO_API_BASE_URL env var
  - fixtures/provider_addresses.yaml — per-provider test addresses
Run: GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/ -m e2e -v

<!-- Locust load tests (from 23-02-SUMMARY) -->
Files in loadtests/:
  - geo_api_locustfile.py — weighted geocode/validate/cascade tasks
  - addresses/cold_cache_addresses.txt — 30 unique addresses
  - addresses/warm_cache_addresses.txt — 10 repeated addresses
Cold run: uv run locust -f loadtests/geo_api_locustfile.py --headless -u 30 -r 0.25 -t 7m --csv=loadtests/reports/cold --host=http://localhost:8000 --address-file=loadtests/addresses/cold_cache_addresses.txt
Warm run: uv run locust -f loadtests/geo_api_locustfile.py --headless -u 30 -r 0.25 -t 7m --csv=loadtests/reports/warm --host=http://localhost:8000 --address-file=loadtests/addresses/warm_cache_addresses.txt

<!-- Observability verification scripts (from 23-03-SUMMARY) -->
Scripts in scripts/verify/:
  - verify_loki.py — queries Loki for structured JSON logs with request_id and trace_id
  - verify_tempo.py — queries Tempo for traces with DB and provider child spans
  - verify_victoriametrics.py — queries VictoriaMetrics for metrics with correct labels
Run: uv run python scripts/verify/verify_loki.py --loki-url http://localhost:3100
     uv run python scripts/verify/verify_tempo.py --tempo-url http://localhost:3200
     uv run python scripts/verify/verify_victoriametrics.py --vm-url http://localhost:8428
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run E2E tests against prod</name>
  <files></files>
  <read_first>
    - tests/e2e/conftest.py
    - tests/e2e/fixtures/provider_addresses.yaml
    - .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-05-SUMMARY.md
    - .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-06-SUMMARY.md
  </read_first>
  <action>
    Run the existing E2E test suite against the deployed prod service. No new code.

    **Step 1: Start port-forward to prod geo-api**
    ```
    kubectl port-forward -n civpulse-prod svc/geo-api 8000:8000 &
    PF_PID=$!
    sleep 3
    ```

    **Step 2: Verify service is reachable**
    ```
    curl -sf http://localhost:8000/health/ready | python3 -m json.tool
    ```
    Confirm geocoding_providers >= 4 before proceeding. If less than 4, STOP -- Plan 05 did not complete successfully.

    **Step 3: Run E2E tests**
    ```
    GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/ -m e2e -v --tb=short
    ```

    Note: If Tiger is not registered (per Plan 06 findings), Tiger-specific test cases may fail. This is expected if Tiger was deferred as a non-blocker. The remaining provider tests (Census, OA, NAD, Macon-Bibb) must pass.

    **Step 4: Kill port-forward**
    ```
    kill $PF_PID
    ```

    Record test results (pass count, fail count, which providers passed).
  </action>
  <verify>
    <automated>GEO_API_BASE_URL=http://localhost:8000 uv run pytest tests/e2e/ -m e2e -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - E2E tests for Census, OA, NAD, and Macon-Bibb providers pass (geocode + validate)
    - Cascade pipeline E2E test passes
    - Any Tiger test failures are expected and documented if Tiger was deferred in Plan 06
  </acceptance_criteria>
  <done>E2E test suite passes for all registered providers. Test results recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run Locust load tests and capture baselines</name>
  <files></files>
  <read_first>
    - loadtests/geo_api_locustfile.py
    - loadtests/addresses/cold_cache_addresses.txt
    - loadtests/addresses/warm_cache_addresses.txt
  </read_first>
  <action>
    Run the existing Locust load tests per D-07 (cold then warm) and D-09 (30 users, 2-min ramp, 5-min sustain).

    **Step 1: Start port-forward**
    ```
    kubectl port-forward -n civpulse-prod svc/geo-api 8000:8000 &
    PF_PID=$!
    sleep 3
    ```

    **Step 2: Cold-cache run**
    ```
    uv run locust -f loadtests/geo_api_locustfile.py \
      --headless -u 30 -r 0.25 -t 7m \
      --csv=loadtests/reports/cold \
      --host=http://localhost:8000
    ```
    Note: The locustfile may use --address-file or ENV var for address file selection. Check the locustfile implementation for the correct parameter. If it reads from an env var:
    ```
    ADDRESS_FILE=loadtests/addresses/cold_cache_addresses.txt uv run locust ...
    ```

    **Step 3: Warm-cache run**
    ```
    uv run locust -f loadtests/geo_api_locustfile.py \
      --headless -u 30 -r 0.25 -t 7m \
      --csv=loadtests/reports/warm \
      --host=http://localhost:8000
    ```
    Similarly use warm_cache_addresses.txt.

    **Step 4: Extract baselines from CSV reports**
    ```
    echo "=== Cold Cache Baselines ==="
    cat loadtests/reports/cold_stats.csv
    echo ""
    echo "=== Warm Cache Baselines ==="
    cat loadtests/reports/warm_stats.csv
    ```
    Record P50, P95, P99 latencies from the Aggregated row.

    **Step 5: Kill port-forward**
    ```
    kill $PF_PID
    ```
  </action>
  <verify>
    <automated>ls -la loadtests/reports/cold_stats.csv loadtests/reports/warm_stats.csv 2>/dev/null && echo "Reports exist" || echo "MISSING reports"</automated>
  </verify>
  <acceptance_criteria>
    - Cold-cache CSV report exists with P50/P95/P99 latency data
    - Warm-cache CSV report exists with P50/P95/P99 latency data
    - No Locust errors above 5% error rate (service was stable under load)
  </acceptance_criteria>
  <done>Locust baseline reports generated for cold-cache and warm-cache runs at 30 concurrent users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run observability verification and review all results</name>
  <files></files>
  <read_first>
    - scripts/verify/verify_loki.py
    - scripts/verify/verify_tempo.py
    - scripts/verify/verify_victoriametrics.py
    - .planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-06-SUMMARY.md
  </read_first>
  <what-built>E2E tests and load tests have been run against the remediated prod environment. Now running observability verification scripts that query Loki, Tempo, and VictoriaMetrics to confirm logs/traces/metrics are flowing correctly under load.</what-built>
  <action>
    CHECKPOINT: Human verification required.
    
    What was built: E2E tests and load tests have been run against the remediated prod environment. Now running observability verification scripts that query Loki, Tempo, and VictoriaMetrics to confirm logs/traces/metrics are flowing correctly under load.
    
    Verification steps:
    1. Start port-forwards to observability backends:
       ```
       kubectl port-forward -n civpulse-infra svc/loki 3100:3100 &
       kubectl port-forward -n civpulse-infra svc/tempo 3200:3200 &
       kubectl port-forward -n civpulse-infra svc/victoriametrics 8428:8428 &
       sleep 3
       ```

    2. Run Loki verification (TEST-04):
       ```
       uv run python scripts/verify/verify_loki.py --loki-url http://localhost:3100
       ```
       Expected: Structured JSON logs with request_id and trace_id fields found.

    3. Run Tempo verification (TEST-05) -- skip if Tempo was deferred in Plan 06:
       ```
       uv run python scripts/verify/verify_tempo.py --tempo-url http://localhost:3200
       ```
       Expected: Traces with DB and provider child spans found.

    4. Run VictoriaMetrics verification (TEST-06):
       ```
       uv run python scripts/verify/verify_victoriametrics.py --vm-url http://localhost:8428
       ```
       Expected: http_requests_total, http_request_duration_seconds, geo_provider_requests_total metrics present.

    5. Kill all port-forwards:
       ```
       kill %1 %2 %3
       ```

    6. Review overall results:
       - How many E2E tests passed vs failed?
       - What are the cold/warm P50/P95/P99 numbers?
       - Which observability checks passed?
  </action>
  <how-to-verify>
    1. Start port-forwards to observability backends:
       ```
       kubectl port-forward -n civpulse-infra svc/loki 3100:3100 &
       kubectl port-forward -n civpulse-infra svc/tempo 3200:3200 &
       kubectl port-forward -n civpulse-infra svc/victoriametrics 8428:8428 &
       sleep 3
       ```

    2. Run Loki verification (TEST-04):
       ```
       uv run python scripts/verify/verify_loki.py --loki-url http://localhost:3100
       ```
       Expected: Structured JSON logs with request_id and trace_id fields found.

    3. Run Tempo verification (TEST-05) -- skip if Tempo was deferred in Plan 06:
       ```
       uv run python scripts/verify/verify_tempo.py --tempo-url http://localhost:3200
       ```
       Expected: Traces with DB and provider child spans found.

    4. Run VictoriaMetrics verification (TEST-06):
       ```
       uv run python scripts/verify/verify_victoriametrics.py --vm-url http://localhost:8428
       ```
       Expected: http_requests_total, http_request_duration_seconds, geo_provider_requests_total metrics present.

    5. Kill all port-forwards:
       ```
       kill %1 %2 %3
       ```

    6. Review overall results:
       - How many E2E tests passed vs failed?
       - What are the cold/warm P50/P95/P99 numbers?
       - Which observability checks passed?
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify - requires manual confirmation"</automated>
  </verify>
  <acceptance_criteria>
    - Loki verification passes (structured logs with request_id and trace_id)
    - VictoriaMetrics verification passes (request rate, latency histograms, error rate metrics)
    - Tempo verification passes OR is documented as deferred per Plan 06 decision
    - All results documented for the final validation pass
  </acceptance_criteria>
  <done>Human verified and approved the results.</done>
  <resume-signal>Type "approved" with a summary of results (E2E pass/fail count, baseline latencies, observability status), or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- E2E tests pass for all registered providers
- Locust CSV reports exist in loadtests/reports/
- Observability verification scripts pass (or are triaged per Plan 06 decisions)
</verification>

<success_criteria>
- TEST-01: E2E tests pass for all registered providers (geocode + validate)
- TEST-02: Cascade pipeline E2E test passes
- TEST-03: Cold-cache and warm-cache Locust baselines captured with P50/P95/P99
- TEST-04: Loki log verification passes
- TEST-05: Tempo trace verification passes or is formally deferred
- TEST-06: VictoriaMetrics metric verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-e2e-testing-load-baselines-and-final-validation/23-07-SUMMARY.md`
</output>
