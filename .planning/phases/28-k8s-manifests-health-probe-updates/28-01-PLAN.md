---
phase: 28-k8s-manifests-health-probe-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/base/nominatim.yaml
  - k8s/base/tile-server.yaml
  - k8s/base/valhalla.yaml
  - k8s/base/osm-pvcs.yaml
  - k8s/base/kustomization.yaml
autonomous: true
requirements: [INFRA-04]
must_haves:
  truths:
    - "kubectl kustomize k8s/base/ renders without errors"
    - "Kustomize output includes Deployments for nominatim, tile-server, valhalla"
    - "Kustomize output includes Services for nominatim, tile-server, valhalla on correct ports"
    - "Kustomize output includes 4 PVCs with exact sizes (nominatim 50Gi, osm-tile 20Gi, valhalla 10Gi, osm-pbf 5Gi)"
    - "Resource requests/limits match CONTEXT exact values"
  artifacts:
    - path: "k8s/base/nominatim.yaml"
      provides: "Nominatim Deployment + ClusterIP Service"
      contains: "kind: Deployment"
    - path: "k8s/base/tile-server.yaml"
      provides: "Tile server Deployment + ClusterIP Service"
      contains: "kind: Deployment"
    - path: "k8s/base/valhalla.yaml"
      provides: "Valhalla Deployment + ClusterIP Service"
      contains: "kind: Deployment"
    - path: "k8s/base/osm-pvcs.yaml"
      provides: "Four PersistentVolumeClaims for OSM sidecars"
      contains: "kind: PersistentVolumeClaim"
    - path: "k8s/base/kustomization.yaml"
      provides: "Updated resources list including OSM sidecars"
      contains: "nominatim.yaml"
  key_links:
    - from: "k8s/base/kustomization.yaml"
      to: "nominatim.yaml, tile-server.yaml, valhalla.yaml, osm-pvcs.yaml"
      via: "resources list entries"
      pattern: "resources:.*nominatim\\.yaml"
---

<objective>
Create Kubernetes manifests for the three OSM sidecar services (Nominatim, tile server, Valhalla) and their PersistentVolumeClaims, wired into the existing Kustomize base. This makes INFRA-04 deployable via `kubectl kustomize k8s/base/`.

Purpose: Allows the OSM sidecars deployed via Docker Compose in Phases 24-27 to be promoted to Kubernetes clusters (dev/prod) using the same Kustomize workflow as the existing geo-api deployment.

Output: 4 new manifest files + updated kustomization.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-k8s-manifests-health-probe-updates/28-CONTEXT.md
@k8s/base/deployment.yaml
@k8s/base/service.yaml
@k8s/base/pvc.yaml
@k8s/base/kustomization.yaml

<interfaces>
<!-- Existing Kustomize conventions (from k8s/base/kustomization.yaml): -->
<!-- commonLabels applied to all resources: -->
<!--   app.kubernetes.io/name: geo-api -->
<!--   app.kubernetes.io/part-of: civpulse-geo -->
<!-- NOTE: commonLabels is DEPRECATED in newer kustomize. Because geo-api's -->
<!-- manifests rely on it for selectors, new manifests MUST set their own -->
<!-- metadata.labels + selector.matchLabels explicitly and NOT rely on -->
<!-- commonLabels to merge app.kubernetes.io/name overrides. Each sidecar -->
<!-- Deployment must use app.kubernetes.io/name: {sidecar-name} in its own -->
<!-- selector and template labels so it does NOT collide with geo-api pods. -->

<!-- Service name -> hostname -> settings key mapping (from src/civpulse_geo/config.py): -->
<!--   nominatim   -> http://nominatim:8080   (osm_nominatim_url) -->
<!--   tile-server -> http://tile-server:8080 (osm_tile_url) -->
<!--   valhalla    -> http://valhalla:8002    (osm_valhalla_url) -->

<!-- Docker Compose image references (from docker-compose.yml in Phase 24): -->
<!--   nominatim:   mediagis/nominatim:4.5 -->
<!--   tile-server: overv/openstreetmap-tile-server:latest -->
<!--   valhalla:    gisops/valhalla:latest -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create osm-pvcs.yaml with 4 PVCs</name>
  <files>k8s/base/osm-pvcs.yaml</files>
  <action>
    Create a single YAML file with 4 PersistentVolumeClaim documents separated by `---`:

    1. `nominatim-data-pvc`: accessModes ReadWriteOnce, storage 50Gi
    2. `osm-tile-data-pvc`: accessModes ReadWriteOnce, storage 20Gi
    3. `valhalla-tiles-pvc`: accessModes ReadWriteOnce, storage 10Gi
    4. `osm-pbf-pvc`: accessModes ReadWriteOnce, storage 5Gi

    Do NOT set storageClassName (per D: let overlays override / cluster default applies).
    Do NOT add labels beyond `metadata.name` — commonLabels in kustomization.yaml will inject them.

    Follow k8s/base/pvc.yaml structure exactly (apiVersion: v1, kind: PersistentVolumeClaim, spec.accessModes, spec.resources.requests.storage).
  </action>
  <verify>
    <automated>kubectl kustomize k8s/base/ 2>&amp;1 | grep -c "kind: PersistentVolumeClaim" | grep -q "^5$"</automated>
  </verify>
  <done>File exists with 4 PVC documents. Kustomize output shows 5 total PVCs (1 ollama + 4 new). Each new PVC has correct storage size.</done>
</task>

<task type="auto">
  <name>Task 2: Create nominatim.yaml, tile-server.yaml, valhalla.yaml Deployments + Services</name>
  <files>k8s/base/nominatim.yaml, k8s/base/tile-server.yaml, k8s/base/valhalla.yaml</files>
  <action>
    Create three manifest files, each containing a Deployment + a ClusterIP Service (separated by `---`).

    **IMPORTANT — label isolation:** Each Deployment MUST override the geo-api `app.kubernetes.io/name` label in its own selector + template labels and Service selector. Otherwise commonLabels from kustomization.yaml will make all sidecars match geo-api's selector.

    **k8s/base/nominatim.yaml:**
    - Deployment `nominatim`, replicas: 1, strategy: Recreate
    - selector.matchLabels: `app.kubernetes.io/name: nominatim`
    - template.metadata.labels: `app.kubernetes.io/name: nominatim`
    - container `nominatim`, image `mediagis/nominatim:4.5`, containerPort 8080
    - volumeMounts: `/var/lib/postgresql/14/main` from `nominatim-data` volume
    - volumes: `nominatim-data` -> persistentVolumeClaim claimName `nominatim-data-pvc`
    - resources: requests `{memory: 4Gi, cpu: 500m}`, limits `{memory: 8Gi, cpu: 2000m}`
    - Service `nominatim`, type ClusterIP, selector `app.kubernetes.io/name: nominatim`, port 8080 targetPort 8080

    **k8s/base/tile-server.yaml:**
    - Deployment `tile-server`, replicas: 1, strategy: Recreate
    - selector.matchLabels + template labels: `app.kubernetes.io/name: tile-server`
    - container `tile-server`, image `overv/openstreetmap-tile-server:latest`, containerPort 80
    - args: ["run"] (tile server run command)
    - volumeMounts: `/data/database` from `osm-tile-data` volume, `/data/osm` from `osm-pbf` volume
    - volumes: `osm-tile-data` -> PVC `osm-tile-data-pvc`; `osm-pbf` -> PVC `osm-pbf-pvc`
    - resources: requests `{memory: 2Gi, cpu: 500m}`, limits `{memory: 4Gi, cpu: 1500m}`
    - Service `tile-server`, type ClusterIP, port 8080 targetPort 80

    **k8s/base/valhalla.yaml:**
    - Deployment `valhalla`, replicas: 1, strategy: Recreate
    - selector.matchLabels + template labels: `app.kubernetes.io/name: valhalla`
    - container `valhalla`, image `gisops/valhalla:latest`, containerPort 8002
    - volumeMounts: `/custom_files` from `valhalla-tiles` volume
    - volumes: `valhalla-tiles` -> PVC `valhalla-tiles-pvc`
    - resources: requests `{memory: 2Gi, cpu: 500m}`, limits `{memory: 4Gi, cpu: 1500m}`
    - Service `valhalla`, type ClusterIP, port 8002 targetPort 8002

    Do NOT add readinessProbe/livenessProbe/startupProbe to these sidecars — their readiness is surfaced via geo-api's /health/ready (plan 02). Keep manifests minimal.
  </action>
  <verify>
    <automated>kubectl kustomize k8s/base/ 2>&amp;1 | grep -E "^kind: (Deployment|Service)$" | sort | uniq -c</automated>
  </verify>
  <done>
    Three files exist. `kubectl kustomize k8s/base/` output contains 4 Deployments (geo-api + 3 sidecars) and 4 Services (geo-api + 3 sidecars). Each sidecar Deployment's selector matches ONLY its own pods (unique app.kubernetes.io/name label). Resource limits match CONTEXT exact values.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update kustomization.yaml resources list</name>
  <files>k8s/base/kustomization.yaml</files>
  <action>
    Append four new entries to the `resources:` list in `k8s/base/kustomization.yaml`:
    - `nominatim.yaml`
    - `tile-server.yaml`
    - `valhalla.yaml`
    - `osm-pvcs.yaml`

    Preserve existing entries (deployment.yaml, service.yaml, configmap.yaml, pvc.yaml) and existing `commonLabels` block. Order: existing entries first, then new OSM entries.
  </action>
  <verify>
    <automated>kubectl kustomize k8s/base/ > /dev/null 2>&amp;1 &amp;&amp; echo "kustomize-ok"</automated>
  </verify>
  <done>
    `kubectl kustomize k8s/base/` exits 0 with no errors. Output is non-empty and includes all sidecar resources. `grep -E "nominatim\\.yaml|tile-server\\.yaml|valhalla\\.yaml|osm-pvcs\\.yaml" k8s/base/kustomization.yaml` finds all 4 lines.
  </done>
</task>

</tasks>

<verification>
Run `kubectl kustomize k8s/base/` and confirm:
1. Exit code 0
2. Output contains: 4 Deployments, 4 Services, 5 PVCs (1 existing ollama-pvc + 4 new OSM PVCs), 1 ConfigMap
3. Sidecar Deployment selectors are distinct (no selector collision with geo-api)
4. Resource limits match CONTEXT exactly
</verification>

<success_criteria>
- `kubectl kustomize k8s/base/` succeeds (INFRA-04)
- All 3 OSM sidecar Deployments + Services + 4 PVCs render in output
- Resource limits/requests match decided values in 28-CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/28-k8s-manifests-health-probe-updates/28-01-SUMMARY.md`
</output>
