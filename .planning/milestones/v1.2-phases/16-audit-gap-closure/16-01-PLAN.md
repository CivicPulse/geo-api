---
phase: 16-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/civpulse_geo/main.py
  - src/civpulse_geo/services/geocoding.py
  - tests/test_geocoding_service.py
  - .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md
autonomous: true
requirements: [FUZZ-02, FUZZ-03, FUZZ-04, FIX-01, FIX-02, FIX-03, FIX-04, FUZZ-01, SPELL-01, SPELL-02, SPELL-03]

must_haves:
  truths:
    - "app.state.fuzzy_matcher is a FuzzyMatcher instance after app startup — cascade stage 3 is reachable at runtime"
    - "_legacy_geocode does not raise ValueError when all local providers return NO_MATCH and CASCADE_ENABLED=false"
    - "Phase 13 VERIFICATION.md exists confirming SPELL-01/02/03 and FUZZ-02/03/04 are implemented and tested"
    - "REQUIREMENTS.md FIX-04 text shows scourgify=0.3 and Tiger=0.4 (already correct — verified, not changed)"
    - "Full test suite passes with no new failures (11 pre-existing failures allowed)"
  artifacts:
    - path: "src/civpulse_geo/main.py"
      provides: "FuzzyMatcher startup wiring in lifespan()"
      contains: "app.state.fuzzy_matcher = FuzzyMatcher"
    - path: "src/civpulse_geo/services/geocoding.py"
      provides: "5-tuple unpack in _legacy_geocode"
      contains: "street_number, street_name, postal_code, _, _ = _parse_input_address"
    - path: "tests/test_geocoding_service.py"
      provides: "Test proving 5-tuple unpack does not raise ValueError"
      contains: "test_legacy_geocode_no_match_does_not_raise_value_error"
    - path: ".planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md"
      provides: "Formal verification of Phase 13 requirements"
      contains: "SPELL-01"
  key_links:
    - from: "src/civpulse_geo/main.py"
      to: "src/civpulse_geo/services/fuzzy.py"
      via: "import FuzzyMatcher + app.state assignment"
      pattern: "from civpulse_geo.services.fuzzy import FuzzyMatcher"
    - from: "src/civpulse_geo/services/geocoding.py"
      to: "src/civpulse_geo/providers/openaddresses.py"
      via: "_parse_input_address 5-tuple return"
      pattern: "street_number, street_name, postal_code, _, _"
---

<objective>
Close all v1.2 milestone audit gaps: wire FuzzyMatcher to app startup so cascade stage 3 fires at runtime, fix the legacy path 3-tuple ValueError, create Phase 13 VERIFICATION.md, and confirm FIX-04 documentation accuracy.

Purpose: FUZZ-02/03/04 are implemented but unreachable because `app.state.fuzzy_matcher` is never assigned. The legacy path has a latent ValueError. These are the last blockers before v1.2 milestone sign-off.
Output: Patched main.py and geocoding.py, new test, Phase 13 VERIFICATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-audit-gap-closure/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/civpulse_geo/services/fuzzy.py:
```python
class FuzzyMatcher:
    def __init__(self, session_factory):
        """Stores session_factory. Stateless init — no DB connection at construction."""
        self._session_factory = session_factory
```

From src/civpulse_geo/providers/openaddresses.py:
```python
def _parse_input_address(
    address: str,
) -> tuple[str | None, str | None, str | None, str | None, str | None]:
    """Returns (street_number, street_name, postal_code, street_suffix, street_directional)"""
```

From src/civpulse_geo/main.py (existing pattern):
```python
# SpellCorrector startup pattern (analogous):
app.state.spell_corrector = load_spell_corrector(conn)

# FuzzyMatcher needs simpler wiring — no sync engine, no try/except:
# app.state.fuzzy_matcher = FuzzyMatcher(AsyncSessionLocal)
```

From src/civpulse_geo/services/geocoding.py line 214 (the bug):
```python
# CURRENT (3-tuple — raises ValueError):
street_number, street_name, postal_code = _parse_input_address(normalized)

# FIXED (5-tuple — discards unused suffix and directional):
street_number, street_name, postal_code, _, _ = _parse_input_address(normalized)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix legacy 5-tuple unpack and add regression test</name>
  <files>src/civpulse_geo/services/geocoding.py, tests/test_geocoding_service.py</files>
  <read_first>
    - src/civpulse_geo/services/geocoding.py (see line 214 for the 3-tuple bug)
    - tests/test_geocoding_service.py (see existing test patterns, imports, mock helpers)
    - src/civpulse_geo/providers/openaddresses.py (see _parse_input_address return signature)
  </read_first>
  <behavior>
    - Test: calling _legacy_geocode with CASCADE_ENABLED=false and all local providers returning confidence=0.0 does NOT raise ValueError — it returns a result dict with local_results
    - Test must exercise the exact code path at geocoding.py line 214 where the unpack occurs
  </behavior>
  <action>
1. Write test FIRST in tests/test_geocoding_service.py:

Add a test function `test_legacy_geocode_no_match_does_not_raise_value_error` that:
- Creates a GeocodingService instance
- Mocks a local provider returning confidence=0.0 (NO_MATCH)
- Patches `civpulse_geo.services.geocoding.settings` with `cascade_enabled = False`
- Calls `service.geocode(freeform="123 XYZZY ST MACON GA 31201", db=db, providers={"test-local": local_provider}, http_client=http_client)`
- Asserts the call does NOT raise ValueError
- Asserts result["local_results"] has entries with confidence 0.0

Follow the existing test patterns in the file — use `@pytest.mark.asyncio`, `AsyncMock`, `MagicMock`, the existing `_make_db_for_local_provider` or similar helper pattern.

2. Run test — confirm it FAILS with `ValueError: too many values to unpack`

3. Fix geocoding.py line 214: change
   `street_number, street_name, postal_code = _parse_input_address(normalized)`
   to
   `street_number, street_name, postal_code, _, _ = _parse_input_address(normalized)`

4. Run test again — confirm it PASSES
  </action>
  <verify>
    <automated>uv run pytest tests/test_geocoding_service.py::test_legacy_geocode_no_match_does_not_raise_value_error -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_geocoding_service.py contains `def test_legacy_geocode_no_match_does_not_raise_value_error`
    - src/civpulse_geo/services/geocoding.py contains `street_number, street_name, postal_code, _, _ = _parse_input_address(normalized)` (the 3-tuple line is gone)
    - `uv run pytest tests/test_geocoding_service.py -x -q` exits 0
  </acceptance_criteria>
  <done>The legacy geocode path handles the 5-tuple return from _parse_input_address without raising ValueError. A regression test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FuzzyMatcher to app startup</name>
  <files>src/civpulse_geo/main.py</files>
  <read_first>
    - src/civpulse_geo/main.py (see lifespan function — spell_corrector and llm_corrector blocks)
    - src/civpulse_geo/services/fuzzy.py (see FuzzyMatcher.__init__ — confirms stateless init)
  </read_first>
  <action>
1. Add import at top of main.py (after the existing `from civpulse_geo.spell import load_spell_corrector` line):
   `from civpulse_geo.services.fuzzy import FuzzyMatcher`

2. Add assignment in lifespan() AFTER the spell_corrector try/except block (line ~94) and BEFORE the LLM corrector block (line ~96). Insert these two lines:
   ```python
   app.state.fuzzy_matcher = FuzzyMatcher(AsyncSessionLocal)
   logger.info("FuzzyMatcher registered")
   ```

No try/except needed — FuzzyMatcher.__init__ only stores session_factory, cannot fail.
No conditional needed — FuzzyMatcher handles empty staging tables gracefully at query time.

3. Run targeted tests to confirm no import cycle or startup regression:
   `uv run pytest tests/test_geocoding_service.py tests/test_fuzzy_matcher.py -x -q`
  </action>
  <verify>
    <automated>uv run pytest tests/test_geocoding_service.py tests/test_fuzzy_matcher.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/civpulse_geo/main.py contains `from civpulse_geo.services.fuzzy import FuzzyMatcher`
    - src/civpulse_geo/main.py contains `app.state.fuzzy_matcher = FuzzyMatcher(AsyncSessionLocal)`
    - src/civpulse_geo/main.py contains `logger.info("FuzzyMatcher registered")`
    - The FuzzyMatcher wiring appears AFTER the spell_corrector block and BEFORE the LLM corrector block
    - `uv run pytest tests/test_geocoding_service.py tests/test_fuzzy_matcher.py -x -q` exits 0
  </acceptance_criteria>
  <done>app.state.fuzzy_matcher is assigned a FuzzyMatcher(AsyncSessionLocal) during startup. Cascade stage 3 is now reachable at runtime, satisfying FUZZ-02/03/04 runtime wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 13 VERIFICATION.md and confirm FIX-04 docs</name>
  <files>.planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-correctness-fixes-and-db-prerequisites/12-VERIFICATION.md (format reference)
    - .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-01-SUMMARY.md (what was built)
    - .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-02-SUMMARY.md (what was built)
    - .planning/REQUIREMENTS.md (confirm FIX-04 line already shows scourgify=0.3, Tiger=0.4)
    - src/civpulse_geo/spell/corrector.py (confirm SpellCorrector exists)
    - src/civpulse_geo/services/fuzzy.py (confirm FuzzyMatcher exists)
    - tests/test_spell_corrector.py (count tests)
    - tests/test_fuzzy_matcher.py (count tests)
    - tests/test_fuzzy_calibration.py (count tests)
  </read_first>
  <action>
1. Run the Phase 13 test files to get exact pass counts:
   `uv run pytest tests/test_spell_corrector.py tests/test_fuzzy_matcher.py tests/test_fuzzy_calibration.py -v -q`

2. Create `.planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md` with:
   - YAML frontmatter: phase, verified date (current timestamp), status: passed
   - Section: "Phase 13: Spell Correction and Fuzzy/Phonetic Matching -- Verification Report"
   - Phase Goal from ROADMAP: "Addresses with typoed or phonetically misspelled street names are recovered before they reach the cascade orchestrator"
   - Observable Truths table with 6 rows (one per requirement):
     - SPELL-01: SpellCorrector.correct_street_name() exists, scoped to street name token only. Evidence: `src/civpulse_geo/spell/corrector.py` contains `def correct_street_name`. Tests: test_spell_corrector.py.
     - SPELL-02: Dictionary built from NAD/OA/Macon-Bibb staging tables + Tiger featnames. Evidence: `src/civpulse_geo/spell/corrector.py` contains `rebuild_dictionary` with TRUNCATE + INSERT from three staging tables. Tests: test_spell_corrector.py.
     - SPELL-03: Dictionary auto-rebuilds on CLI load commands. Evidence: `src/civpulse_geo/cli/__init__.py` calls `rebuild_dictionary(conn)` in load-oa, load-nad, gis import hooks. Tests: test_spell_corrector.py.
     - FUZZ-02: FuzzyMatcher uses word_similarity() with threshold 0.65-0.70. Evidence: `src/civpulse_geo/services/fuzzy.py` contains `word_similarity` SQL with threshold constant. Tests: test_fuzzy_matcher.py. Note: startup wiring added in Phase 16 Task 2.
     - FUZZ-03: dmetaphone() tiebreaker for phonetically ambiguous candidates. Evidence: `src/civpulse_geo/services/fuzzy.py` contains `dmetaphone` SQL query as tiebreaker. Tests: test_fuzzy_matcher.py.
     - FUZZ-04: Calibration corpus passes. Evidence: `tests/test_fuzzy_calibration.py` with 30 parameterized entries. Tests: test_fuzzy_calibration.py (30 tests pass).

3. Confirm FIX-04 documentation: Read `.planning/REQUIREMENTS.md` line for FIX-04. It should already say "Scourgify validation confidence reduced from 1.0 to 0.3; Tiger validation confidence reduced from 1.0 to 0.4". If it does, document this as verified (no change needed). If it says something different (e.g., "0.5"), update it to match the actual code constants (SCOURGIFY_CONFIDENCE=0.3, TIGER_VALIDATION_CONFIDENCE=0.4 per D-09/D-10).

4. Run full test suite to confirm no new failures:
   `uv run pytest -x -q`
   Expect 505+ passed, 11 failed (pre-existing). Zero new failures.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md && grep -q "SPELL-01" .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md && grep -q "FUZZ-04" .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md` exists
    - File contains "SPELL-01", "SPELL-02", "SPELL-03", "FUZZ-02", "FUZZ-03", "FUZZ-04"
    - File contains "status: passed" in frontmatter
    - File contains "correct_street_name" (evidence for SPELL-01)
    - File contains "word_similarity" (evidence for FUZZ-02)
    - File contains "dmetaphone" (evidence for FUZZ-03)
    - File contains "test_fuzzy_calibration" (evidence for FUZZ-04)
    - `.planning/REQUIREMENTS.md` FIX-04 line contains "0.3" and "0.4" (already true — verified not changed)
    - `uv run pytest -x -q` shows no new failures beyond the 11 pre-existing ones
  </acceptance_criteria>
  <done>Phase 13 VERIFICATION.md exists confirming all 6 requirements (SPELL-01/02/03, FUZZ-02/03/04) are implemented and tested. FIX-04 documentation accuracy confirmed. Full test suite shows no regressions.</done>
</task>

</tasks>

<verification>
1. `grep -n "fuzzy_matcher = FuzzyMatcher" src/civpulse_geo/main.py` — confirms startup wiring
2. `grep -n "postal_code, _, _ = _parse_input_address" src/civpulse_geo/services/geocoding.py` — confirms 5-tuple fix
3. `test -f .planning/phases/13-spell-correction-and-fuzzy-phonetic-matching/13-VERIFICATION.md` — confirms verification artifact
4. `uv run pytest -x -q` — full suite green (11 pre-existing failures, zero new)
</verification>

<success_criteria>
- FuzzyMatcher is wired to app.state at startup — cascade stage 3 is reachable
- _legacy_geocode handles 5-tuple without ValueError
- Phase 13 VERIFICATION.md exists with all 6 requirement confirmations
- FIX-04 documentation verified correct
- Full test suite: no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/16-audit-gap-closure/16-01-SUMMARY.md`
</output>
