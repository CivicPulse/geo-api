---
phase: 11-fix-batch-local-serialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Both batch regression tests (test_batch_geocode_local_results_included, test_batch_validate_local_candidates_included) pass"
    - "All 16 batch endpoint tests pass without regression"
    - "ROADMAP.md Phase 11 and plan 11-01 checkboxes are marked complete"
    - "STATE.md reflects Phase 11 completion and GAP-INT-01 closure"
    - "SUMMARY.md documents the pre-applied fix from commit f6f904d"
  artifacts:
    - path: ".planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md"
      provides: "Phase 11 close-out documentation for GAP-INT-01"
      min_lines: 30
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase progress with Phase 11 marked complete"
      contains: "[x] **Phase 11"
    - path: ".planning/STATE.md"
      provides: "Updated project state reflecting Phase 11 completion"
      contains: "Phase 11"
  key_links:
    - from: ".planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md"
      to: "src/civpulse_geo/api/geocoding.py"
      via: "documents fix applied in commit f6f904d"
      pattern: "f6f904d"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/11-fix-batch-local-serialization/11-01-PLAN.md"
      via: "plan checkbox reference"
      pattern: "11-01-PLAN.md"
---

<objective>
Verify that the GAP-INT-01 fix (commit f6f904d) is working correctly and close out Phase 11 with proper documentation updates.

Purpose: The batch endpoint local provider serialization fix was already applied before planning began. This plan confirms the fix works via test execution and formally closes the gap through documentation.
Output: Passing regression tests, updated ROADMAP.md/STATE.md, and 11-01-SUMMARY.md
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-fix-batch-local-serialization/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GAP-INT-01 regression tests pass</name>
  <files>
    tests/test_batch_geocoding_api.py
    tests/test_batch_validation_api.py
  </files>
  <read_first>
    tests/test_batch_geocoding_api.py
    tests/test_batch_validation_api.py
    .planning/phases/11-fix-batch-local-serialization/11-RESEARCH.md
  </read_first>
  <action>
Run the two batch test files to confirm all tests pass, including the two GAP-INT-01 regression tests added in commit f6f904d.

Step 1: Run the targeted regression tests only:
```
uv run pytest tests/test_batch_geocoding_api.py::test_batch_geocode_local_results_included tests/test_batch_validation_api.py::test_batch_validate_local_candidates_included -v
```
Both must PASS.

Step 2: Run the full batch test suites:
```
uv run pytest tests/test_batch_geocoding_api.py tests/test_batch_validation_api.py -v
```
All 16 tests must PASS (no failures, no errors).

Step 3: Run the full project test suite to confirm no regressions:
```
uv run pytest --no-header -q
```
Expected: 325 passed, 10 failed (pre-existing test_import_cli.py failures due to missing data/SAMPLE_Address_Points.geojson fixture — these predate v1.1 and are NOT caused by Phase 11), 2 skipped.

If any batch test fails, STOP and report the failure. Do NOT proceed to Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_batch_geocoding_api.py tests/test_batch_validation_api.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_batch_geocoding_api.py::test_batch_geocode_local_results_included -v` exits 0
    - `uv run pytest tests/test_batch_validation_api.py::test_batch_validate_local_candidates_included -v` exits 0
    - `uv run pytest tests/test_batch_geocoding_api.py tests/test_batch_validation_api.py -v` shows "16 passed" with exit code 0
  </acceptance_criteria>
  <done>Both GAP-INT-01 regression tests pass. All 16 batch endpoint tests pass. No regressions introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md, STATE.md, and write SUMMARY.md to close Phase 11</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/11-fix-batch-local-serialization/11-RESEARCH.md
  </read_first>
  <action>
Update three documentation files to formally close GAP-INT-01 and Phase 11.

**A. Update ROADMAP.md:**

1. Change the Phase 11 bullet from unchecked to checked:
   FROM: `- [ ] **Phase 11: Fix Batch Endpoint Local Provider Serialization**`
   TO:   `- [x] **Phase 11: Fix Batch Endpoint Local Provider Serialization** — Batch endpoints include local provider results in every response item (completed 2026-03-24)`

2. Change the plan checkbox from unchecked to checked:
   FROM: `- [ ] 11-01-PLAN.md — Add local_results/local_candidates serialization to _geocode_one() and _validate_one()`
   TO:   `- [x] 11-01-PLAN.md — Verify GAP-INT-01 fix and close phase documentation`

3. Add Phase 11 to the Progress table:
   Add row: `| 11. Fix Batch Local Serialization | 1/1 | Complete | 2026-03-24 | - |`

4. Update the Execution Order line to include Phase 11:
   FROM: `**Execution Order:** Phases execute in numeric order: 7 → 8 → 9 → 10`
   TO:   `**Execution Order:** Phases execute in numeric order: 7 → 8 → 9 → 10 → 11`

**B. Update STATE.md:**

1. Update frontmatter:
   - `status: complete`
   - `stopped_at: Completed 11-01-PLAN.md`
   - `last_updated:` to current ISO timestamp
   - `total_phases: 5`
   - `completed_phases: 5`
   - `total_plans: 9`
   - `completed_plans: 9`

2. Update "Current Position" section:
   FROM: `Phase: 10 (NAD Provider) — EXECUTING` / `Plan: 2 of 2 (Plan 01 complete)`
   TO:   `Phase: 11 (Fix Batch Local Serialization) — COMPLETE` / `Plan: 1 of 1 (complete)`

3. Update "Current focus" line in Project Reference:
   FROM: `**Current focus:** Phase 10 — NAD Provider`
   TO:   `**Current focus:** v1.1 milestone complete — all phases delivered`

4. Add to Accumulated Context > Decisions:
   `- [Phase 11]: GAP-INT-01 closed — batch endpoints now serialize local_results/local_candidates identically to single-address endpoints (fix applied in f6f904d)`

**C. Write 11-01-SUMMARY.md:**

Create the summary file following the project's established SUMMARY.md frontmatter convention (see 10-02-SUMMARY.md for pattern). Include:

Frontmatter fields:
- `phase: 11-fix-batch-local-serialization`
- `plan: 01`
- `subsystem: api`
- `tags: [batch, serialization, gap-closure, local-providers]`
- `requires:` section referencing Phase 7 pipeline bypass and Phase 10 completion
- `provides:` list including: "Verified GAP-INT-01 fix from commit f6f904d", "16/16 batch endpoint tests passing including 2 regression tests", "Phase 11 documentation close-out"
- `affects: [batch-api-consumers]`
- `tech-stack:` section with `added: []` and `patterns: []` (no new tech)
- `key-files:` section listing `modified: []` and `created: [.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md]`
- `key-decisions:` with "GAP-INT-01 was fixed in commit f6f904d before planning began; Phase 11 is verification-only"
- `patterns-established: []`
- `requirements-completed: []`
- `gaps-closed: [GAP-INT-01]`

Body content:
- Brief description of what GAP-INT-01 was (batch endpoints dropped local_results/local_candidates)
- What commit f6f904d fixed (_geocode_one and _validate_one now pass local_results= and local_candidates= to response constructors)
- Test evidence (16/16 batch tests pass, 2 regression tests specifically cover the gap)
- Files modified by the fix: src/civpulse_geo/api/geocoding.py, src/civpulse_geo/api/validation.py, tests/test_batch_geocoding_api.py, tests/test_batch_validation_api.py
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*Phase 11' .planning/ROADMAP.md && grep -c 'GAP-INT-01' .planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md && grep -c 'Phase 11' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `[x] **Phase 11: Fix Batch Endpoint Local Provider Serialization**`
    - `.planning/ROADMAP.md` contains `[x] 11-01-PLAN.md`
    - `.planning/ROADMAP.md` Progress table contains `| 11. Fix Batch Local Serialization`
    - `.planning/STATE.md` contains `status: complete`
    - `.planning/STATE.md` contains `completed_phases: 5`
    - `.planning/STATE.md` contains `completed_plans: 9`
    - `.planning/STATE.md` contains `Phase: 11 (Fix Batch Local Serialization) — COMPLETE`
    - `.planning/STATE.md` contains `GAP-INT-01 closed`
    - `.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md` exists and contains `f6f904d`
    - `.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md` contains `GAP-INT-01`
    - `.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md` contains `gaps-closed`
  </acceptance_criteria>
  <done>ROADMAP.md shows Phase 11 and plan 11-01 as complete. STATE.md reflects v1.1 milestone complete with 5/5 phases done. SUMMARY.md documents the GAP-INT-01 closure with commit reference and test evidence.</done>
</task>

</tasks>

<verification>
Phase 11 is complete when:
1. `uv run pytest tests/test_batch_geocoding_api.py tests/test_batch_validation_api.py -v` shows 16/16 passed
2. `grep '\[x\] \*\*Phase 11' .planning/ROADMAP.md` returns a match
3. `grep 'status: complete' .planning/STATE.md` returns a match
4. `.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md` exists and contains GAP-INT-01 closure documentation
</verification>

<success_criteria>
- All 16 batch endpoint tests pass (0 failures)
- Both GAP-INT-01 regression tests individually pass
- ROADMAP.md Phase 11 checkbox is marked [x]
- STATE.md reflects all 5 v1.1 phases complete
- SUMMARY.md formally closes GAP-INT-01 with commit f6f904d reference
</success_criteria>

<output>
After completion, the summary already exists as `.planning/phases/11-fix-batch-local-serialization/11-01-SUMMARY.md` (created in Task 2).
</output>
