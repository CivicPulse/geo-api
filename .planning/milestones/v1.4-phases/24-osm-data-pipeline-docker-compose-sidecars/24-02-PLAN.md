---
phase: 24-osm-data-pipeline-docker-compose-sidecars
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - scripts/30_osm_setup.sh
  - src/civpulse_geo/config.py
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "Running `docker compose config -q` exits 0 (compose file is valid)"
    - "Running `docker compose config --services` lists osm-postgres, nominatim, tile-server, valhalla in addition to existing services"
    - "Running `docker compose --profile osm config --services` lists exactly 4 OSM services plus the default api/db"
    - "Settings object has osm_nominatim_url, osm_tile_url, osm_valhalla_url attributes"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Four new services under profiles: [osm]"
      contains: "profiles:"
      min_lines: 100
    - path: "scripts/30_osm_setup.sh"
      provides: "osm-postgres init script installing PostGIS + hstore extensions on nominatim DB"
      contains: "CREATE EXTENSION IF NOT EXISTS postgis"
    - path: "src/civpulse_geo/config.py"
      provides: "OSM service URL settings"
      contains: "osm_nominatim_url"
  key_links:
    - from: "docker-compose.yml nominatim service"
      to: "docker-compose.yml osm-postgres service"
      via: "depends_on service_healthy + NOMINATIM_DATABASE_DSN"
      pattern: "host=osm-postgres"
    - from: "docker-compose.yml osm-postgres"
      to: "scripts/30_osm_setup.sh"
      via: "volume mount /docker-entrypoint-initdb.d/"
      pattern: "30_osm_setup.sh:/docker-entrypoint-initdb.d/"
---

<objective>
Add four Docker Compose services (`osm-postgres`, `nominatim`, `tile-server`, `valhalla`) under `--profile osm`, create the `osm-postgres` init script, and add OSM service URL settings. This establishes the local dev infrastructure that CLI commands (Plans 03-05) will drive.

Purpose: Satisfies INFRA-01 (Nominatim sidecar), INFRA-02 (Valhalla sidecar with persistent graph volume), INFRA-03 (tile server sidecar). Implements D-01, D-02, D-03, D-04, D-09 locked decisions.

Output: Valid docker-compose.yml with 4 new services + 4 named volumes, init script, config.py with 3 new settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-CONTEXT.md
@.planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-RESEARCH.md
@docker-compose.yml
@scripts/20_tiger_setup.sh
@src/civpulse_geo/config.py

<interfaces>
<!-- Existing docker-compose.yml structure (services: db, ollama, api; volumes: postgres_data, ollama_data) -->
<!-- ollama already uses profiles: [llm] — follow same pattern for [osm] -->
<!-- Existing init script pattern from scripts/20_tiger_setup.sh uses psql heredoc -->

From src/civpulse_geo/config.py (Settings class uses pydantic-settings BaseSettings with env_file=".env"):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", arbitrary_types_allowed=True)
    # existing fields: database_url, log_level, environment, etc.
    # existing URL pattern: ollama_url: str = "http://ollama:11434"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create osm-postgres init script</name>
  <files>scripts/30_osm_setup.sh</files>
  <read_first>
    - scripts/20_tiger_setup.sh (EXACT reference pattern — shebang, set -e, psql heredoc, echo)
    - .planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-RESEARCH.md (Pattern 1: osm-postgres Init Script, lines 222-241)
  </read_first>
  <action>
    Create `scripts/30_osm_setup.sh` (mode 755) with EXACTLY this content:

    ```bash
    #!/bin/bash
    # Install PostGIS, hstore, and postgis_topology extensions on osm-postgres.
    # This script runs via /docker-entrypoint-initdb.d/ during PostgreSQL initdb
    # on first startup of the osm-postgres container only.
    #
    # Phase 24 — creates extensions on the `nominatim` database.
    # Mirrors the pattern from scripts/20_tiger_setup.sh.
    #
    # To re-run after modifying this script, destroy the volume:
    #   docker compose --profile osm down -v && docker compose --profile osm up -d osm-postgres
    set -e

    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
        CREATE EXTENSION IF NOT EXISTS postgis;
        CREATE EXTENSION IF NOT EXISTS hstore;
        CREATE EXTENSION IF NOT EXISTS postgis_topology;
    EOSQL

    echo "OSM PostgreSQL extensions installed on database $POSTGRES_DB."
    ```

    Then `chmod +x scripts/30_osm_setup.sh`.

    Note: POSTGRES_USER/POSTGRES_DB env vars are provided by the postgis/postgis image entrypoint from the service's `environment:` block in docker-compose.yml (set to `nominatim` by Task 2).
  </action>
  <verify>
    <automated>test -x scripts/30_osm_setup.sh && bash -n scripts/30_osm_setup.sh && grep -q "CREATE EXTENSION IF NOT EXISTS postgis" scripts/30_osm_setup.sh && grep -q "CREATE EXTENSION IF NOT EXISTS hstore" scripts/30_osm_setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/30_osm_setup.sh` exists and is executable (`test -x`)
    - `bash -n scripts/30_osm_setup.sh` exits 0 (valid bash syntax)
    - `grep -c "CREATE EXTENSION IF NOT EXISTS postgis" scripts/30_osm_setup.sh` returns `1`
    - `grep -c "CREATE EXTENSION IF NOT EXISTS hstore" scripts/30_osm_setup.sh` returns `1`
    - `grep -c "CREATE EXTENSION IF NOT EXISTS postgis_topology" scripts/30_osm_setup.sh` returns `1`
    - `grep -c "set -e" scripts/30_osm_setup.sh` returns `1`
    - `head -1 scripts/30_osm_setup.sh` equals `#!/bin/bash`
  </acceptance_criteria>
  <done>Init script exists, executable, passes bash syntax check, creates 3 required extensions.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 OSM services to docker-compose.yml under --profile osm</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current structure — db service, ollama service uses profiles: [llm], volumes section)
    - .planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-RESEARCH.md (lines 130-220 — exact service definitions; lines 347-360 — Critical Architectural Finding: tile-server uses its OWN internal PostgreSQL, not osm-postgres)
  </read_first>
  <action>
    Append the following 4 services to `docker-compose.yml` AFTER the existing `api` service (before the `volumes:` section). All 4 services MUST use `profiles: [osm]` per D-01.

    IMPORTANT per research Critical Finding: tile-server does NOT connect to osm-postgres. It uses its own internal PostgreSQL via the `osm_tile_data` named volume. osm-postgres hosts only the `nominatim` database.

    Add these services:

    ```yaml
      osm-postgres:
        image: postgis/postgis:17-3.5
        environment:
          POSTGRES_DB: nominatim
          POSTGRES_USER: nominatim
          POSTGRES_PASSWORD: nominatim
        volumes:
          - osm_postgres_data:/var/lib/postgresql/data
          - ./scripts/30_osm_setup.sh:/docker-entrypoint-initdb.d/30_osm_setup.sh
        ports:
          - "5463:5432"
        mem_limit: 4g
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U nominatim -d nominatim"]
          interval: 10s
          timeout: 5s
          retries: 5
        profiles:
          - osm

      nominatim:
        image: mediagis/nominatim:5.2
        environment:
          NOMINATIM_DATABASE_DSN: "pgsql:host=osm-postgres;port=5432;dbname=nominatim;user=nominatim;password=nominatim"
          PBF_PATH: /nominatim/pbf/georgia-latest.osm.pbf
          REPLICATION_URL: ""
        volumes:
          - nominatim_data:/nominatim/data
          - ./data/osm:/nominatim/pbf:ro
        ports:
          - "8093:8080"
        depends_on:
          osm-postgres:
            condition: service_healthy
        healthcheck:
          test: ["CMD-SHELL", "curl -sf http://localhost:8080/status || exit 1"]
          interval: 30s
          timeout: 10s
          retries: 10
          start_period: 600s
        profiles:
          - osm

      tile-server:
        image: overv/openstreetmap-tile-server:2.3.0
        # NOTE: This container uses its own internal PostgreSQL (db: gis, user: renderer).
        # It does NOT connect to osm-postgres. Isolation from civpulse_geo is via separate volume.
        volumes:
          - osm_tile_data:/data/database/
          - osm_tile_cache:/data/tiles/
          - ./data/osm:/data/osm:ro
        ports:
          - "8094:8080"
        command: run
        healthcheck:
          test: ["CMD-SHELL", "curl -sf http://localhost:8080/tile/0/0/0.png || exit 1"]
          interval: 30s
          timeout: 10s
          retries: 5
          start_period: 300s
        profiles:
          - osm

      valhalla:
        image: ghcr.io/valhalla/valhalla:latest
        environment:
          serve_tiles: "True"
          use_tiles_ignore_pbf: "True"
          build_admins: "False"
          build_elevation: "False"
          force_rebuild: "False"
        volumes:
          - valhalla_tiles:/custom_files
          - ./data/osm:/custom_files/pbf:ro
        ports:
          - "8002:8002"
        healthcheck:
          test: ["CMD-SHELL", "curl -sf http://localhost:8002/status || exit 1"]
          interval: 30s
          timeout: 10s
          retries: 5
          start_period: 60s
        profiles:
          - osm
    ```

    Then update the `volumes:` section at the bottom of the file to include 4 new named volumes:

    ```yaml
    volumes:
      postgres_data:
      ollama_data:
      osm_postgres_data:
      nominatim_data:
      osm_tile_data:
      osm_tile_cache:
      valhalla_tiles:
    ```

    After editing, validate with `docker compose config -q` (exits 0 if valid).
  </action>
  <verify>
    <automated>docker compose config -q && docker compose --profile osm config --services | sort | grep -E "^(osm-postgres|nominatim|tile-server|valhalla)$" | wc -l | grep -q "^4$"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0 (compose file is valid YAML and valid schema)
    - `docker compose --profile osm config --services` includes all of: `osm-postgres`, `nominatim`, `tile-server`, `valhalla`
    - `docker compose config --services` (NO profile flag) does NOT include `nominatim`, `tile-server`, `valhalla`, or `osm-postgres` (they are profile-gated)
    - `grep -c "profiles:" docker-compose.yml` returns at least `5` (ollama [llm] + 4 new osm services)
    - `grep -c "image: postgis/postgis:17-3.5" docker-compose.yml` returns `2` (existing db + new osm-postgres)
    - `grep -c "image: mediagis/nominatim:5.2" docker-compose.yml` returns `1`
    - `grep -c "image: overv/openstreetmap-tile-server:2.3.0" docker-compose.yml` returns `1`
    - `grep -c "image: ghcr.io/valhalla/valhalla:latest" docker-compose.yml` returns `1`
    - `grep -c "NOMINATIM_DATABASE_DSN" docker-compose.yml` returns `1`
    - `grep "host=osm-postgres" docker-compose.yml` is present (Nominatim points at osm-postgres, NOT civpulse_geo)
    - `grep -c "civpulse_geo" docker-compose.yml` remains unchanged from pre-edit (Nominatim DSN does NOT reference civpulse_geo — Pitfall 1)
    - `grep -c "osm_postgres_data" docker-compose.yml` returns at least `2` (volume mount + volumes section)
    - `grep -c "valhalla_tiles" docker-compose.yml` returns at least `2`
    - `grep -c "osm_tile_data" docker-compose.yml` returns at least `2`
    - `grep -c "nominatim_data" docker-compose.yml` returns at least `2`
  </acceptance_criteria>
  <done>`docker compose config -q` passes; 4 OSM services exist under `profiles: [osm]`; named volumes declared; Nominatim DSN isolated from civpulse_geo.</done>
</task>

<task type="auto">
  <name>Task 3: Add OSM service URL settings to config.py</name>
  <files>src/civpulse_geo/config.py</files>
  <read_first>
    - src/civpulse_geo/config.py (current Settings class — find where ollama_url is defined to place new fields nearby)
    - .planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-RESEARCH.md (lines 487-493 — Settings Additions)
  </read_first>
  <action>
    Edit `src/civpulse_geo/config.py`. After the existing `ollama_url: str = "http://ollama:11434"` line (inside the Settings class), add these three new settings fields:

    ```python
        # OSM sidecar service URLs (Phase 24 — INFRA-01, INFRA-02, INFRA-03)
        osm_nominatim_url: str = "http://nominatim:8080"
        osm_tile_url: str = "http://tile-server:8080"
        osm_valhalla_url: str = "http://valhalla:8002"
    ```

    Do NOT remove or modify any existing fields. Place the comment and fields as a logical group after the LLM/ollama block.

    Then verify by running:
    ```bash
    uv run python -c "from civpulse_geo.config import settings; print(settings.osm_nominatim_url, settings.osm_tile_url, settings.osm_valhalla_url)"
    ```
    Expected output: `http://nominatim:8080 http://tile-server:8080 http://valhalla:8002`
  </action>
  <verify>
    <automated>uv run python -c "from civpulse_geo.config import settings; assert settings.osm_nominatim_url == 'http://nominatim:8080'; assert settings.osm_tile_url == 'http://tile-server:8080'; assert settings.osm_valhalla_url == 'http://valhalla:8002'; print('OK')" && uv run ruff check src/civpulse_geo/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "osm_nominatim_url" src/civpulse_geo/config.py` returns `1`
    - `grep -c "osm_tile_url" src/civpulse_geo/config.py` returns `1`
    - `grep -c "osm_valhalla_url" src/civpulse_geo/config.py` returns `1`
    - `grep -c 'http://nominatim:8080' src/civpulse_geo/config.py` returns `1`
    - `grep -c 'http://tile-server:8080' src/civpulse_geo/config.py` returns `1`
    - `grep -c 'http://valhalla:8002' src/civpulse_geo/config.py` returns `1`
    - `uv run python -c "from civpulse_geo.config import settings; print(settings.osm_nominatim_url)"` prints `http://nominatim:8080`
    - `uv run ruff check src/civpulse_geo/config.py` exits 0
  </acceptance_criteria>
  <done>Settings class has 3 new OSM URL fields with correct defaults matching docker-compose service names.</done>
</task>

</tasks>

<verification>
- `docker compose config -q` exits 0 (valid compose file)
- `docker compose --profile osm config --services` lists all 4 OSM services
- `uv run pytest tests/ -x --ff -q` all green (no regressions)
- `uv run ruff check src/civpulse_geo/config.py scripts/ docker-compose.yml` no errors
</verification>

<success_criteria>
- Four OSM sidecar services defined under `--profile osm` (INFRA-01, INFRA-02, INFRA-03)
- `osm-postgres` uses init script; hosts `nominatim` database isolated from `civpulse_geo`
- Nominatim connects to `osm-postgres` via libpq DSN format (`pgsql:host=...;...`)
- Valhalla has `valhalla_tiles` persistent volume (D-02, Pitfall 3 — graph built once)
- Config settings expose 3 new OSM service URLs
</success_criteria>

<output>
After completion, create `.planning/phases/24-osm-data-pipeline-docker-compose-sidecars/24-02-SUMMARY.md`
</output>
