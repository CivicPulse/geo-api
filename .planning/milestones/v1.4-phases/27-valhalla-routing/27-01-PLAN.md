---
phase: 27-valhalla-routing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_api_route.py
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "Tests describe GET /route contract for pedestrian mode"
    - "Tests describe GET /route contract for auto mode"
    - "Tests describe RouteResponse schema (maneuvers, polyline, duration, distance)"
    - "Tests describe error paths: 400, 404, 502, 503"
    - "All tests FAIL initially (RED phase) — no route.py exists yet"
  artifacts:
    - path: "tests/test_api_route.py"
      provides: "Contract tests for /route endpoint"
      min_lines: 200
      contains: "AsyncClient"
  key_links:
    - from: "tests/test_api_route.py"
      to: "civpulse_geo.main.app"
      via: "ASGITransport"
      pattern: "from civpulse_geo.main import app"
    - from: "tests/test_api_route.py"
      to: "app.state.valhalla_enabled"
      via: "monkeypatch of app state"
      pattern: "app\\.state\\.valhalla_enabled"
---

<objective>
Write failing contract tests for GET /route endpoint covering pedestrian and auto modes, response schema, and all error paths.

Purpose: TDD RED phase. Establish contract before implementation so Plan 02 has a concrete target to satisfy.
Output: tests/test_api_route.py with ~10 contract tests, all failing with ImportError or 404 (route not yet mounted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-valhalla-routing/27-CONTEXT.md
@tests/test_api_poi_search.py
@tests/test_api_tiles.py
@src/civpulse_geo/api/poi.py

<interfaces>
<!-- Patterns to mirror from Phase 25/26 tests -->

Test harness pattern (from tests/test_api_poi_search.py):
```python
from unittest.mock import AsyncMock, MagicMock
import httpx
from httpx import AsyncClient, ASGITransport
from civpulse_geo.main import app

transport = ASGITransport(app=app)
async with AsyncClient(transport=transport, base_url="http://test") as client:
    response = await client.get("/route", params={"start": "33.7490,-84.3880", "end": "33.7701,-84.3876", "mode": "pedestrian"})
```

Valhalla mock response shape (POST /route upstream):
```json
{
  "trip": {
    "legs": [{
      "shape": "encoded_polyline6_string",
      "summary": {"time": 124.5, "length": 0.83},
      "maneuvers": [
        {"instruction": "Walk east on Peachtree St", "length": 0.12, "time": 18.0, "type": 1},
        {"instruction": "Turn right onto 5th St", "length": 0.71, "time": 106.5, "type": 10}
      ]
    }],
    "summary": {"time": 124.5, "length": 0.83}
  }
}
```

Expected RouteResponse JSON (from Plan 02):
```json
{
  "mode": "pedestrian",
  "polyline": "encoded_polyline6_string",
  "duration_seconds": 124.5,
  "distance_meters": 830.0,
  "maneuvers": [
    {"instruction": "...", "distance_meters": 120.0, "duration_seconds": 18.0, "type": 1}
  ],
  "raw_valhalla": {...}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 10 contract tests for GET /route</name>
  <files>tests/test_api_route.py</files>
  <action>
    Create tests/test_api_route.py with 10 contract tests using AsyncClient + ASGITransport pattern (mirror tests/test_api_poi_search.py).

    Each test MUST monkeypatch `app.state.valhalla_enabled = True` and patch `app.state.http_client.post` with AsyncMock (unless otherwise stated). Valid Georgia coordinates: start="33.7490,-84.3880", end="33.7701,-84.3876".

    Required tests (names must match exactly):

    1. `test_route_pedestrian_returns_200_with_route_response` — mode=pedestrian, mocked Valhalla returns trip with 2 maneuvers; assert 200, response["mode"] == "pedestrian", len(response["maneuvers"]) == 2, response["polyline"] is non-empty string, response["duration_seconds"] > 0, response["distance_meters"] > 0 (ROUTE-01, ROUTE-03).

    2. `test_route_auto_returns_200_with_route_response` — mode=auto, mocked Valhalla trip; assert 200, response["mode"] == "auto" (ROUTE-02).

    3. `test_route_response_converts_km_to_meters` — Valhalla mock returns summary.length=1.5 (km); assert response["distance_meters"] == 1500.0 (ROUTE-03).

    4. `test_route_maneuver_schema_contains_required_fields` — assert each maneuver has keys: instruction, distance_meters, duration_seconds, type (all present, correct types: str, float, float, int) (ROUTE-03).

    5. `test_route_missing_start_returns_422` — omit start query param; assert 422.

    6. `test_route_invalid_start_format_returns_400` — start="not-a-coord"; assert 400, detail mentions "start" or "format".

    7. `test_route_invalid_mode_returns_400` — mode="walking" (not in {pedestrian, auto}); assert 400.

    8. `test_route_same_start_and_end_returns_400` — start == end; assert 400.

    9. `test_route_valhalla_empty_returns_404` — mocked Valhalla returns {"error": "No path"} with HTTP 400 OR empty trip; assert 404, detail mentions "no route" or "not found".

    10. `test_route_upstream_timeout_returns_502` — mock post raises httpx.TimeoutException; assert 502.

    11. `test_route_valhalla_disabled_returns_503` — monkeypatch `app.state.valhalla_enabled = False` before request; assert 503, detail mentions "valhalla" or "disabled".

    Use `_mock_valhalla_response(json_data, status_code=200)` helper similar to `_mock_nominatim_search_response`. Verify the POST body sent to Valhalla matches exactly: `{"locations": [{"lat": 33.7490, "lon": -84.3880}, {"lat": 33.7701, "lon": -84.3876}], "costing": "pedestrian", "units": "kilometers"}` in at least one assertion test (spot-check in test 1).

    All tests are async (`@pytest.mark.asyncio`). All tests MUST fail initially because /route endpoint does not exist yet (404) or ImportError on route module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_route.py -x 2>&1 | grep -E "FAILED|ERROR|passed|failed" | head -20</automated>
  </verify>
  <done>tests/test_api_route.py exists with 11 tests, all FAILING (expected RED state) — either 404 responses or AttributeError on missing route module. File passes ruff check.</done>
</task>

</tasks>

<verification>
- `uv run ruff check tests/test_api_route.py` passes with no errors
- `uv run pytest tests/test_api_route.py --collect-only -q` lists 11 tests
- `uv run pytest tests/test_api_route.py 2>&1 | tail -5` shows all tests failing (RED)
</verification>

<success_criteria>
- 11 contract tests written covering ROUTE-01, ROUTE-02, ROUTE-03
- Tests use AsyncClient + ASGITransport (matches project pattern)
- Tests mock `app.state.http_client.post` for Valhalla calls
- Tests assert exact POST body shape sent to Valhalla `/route`
- All tests currently FAIL (RED phase confirmed)
</success_criteria>

<output>
Create `.planning/phases/27-valhalla-routing/27-01-SUMMARY.md` documenting: test count, expected failures, patterns reused from Phase 25/26 tests.
</output>
