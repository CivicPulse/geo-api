---
phase: 26-nominatim-provider-reverse-geocoding-poi-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_nominatim_provider.py
  - tests/test_api_reverse_geocode.py
  - tests/test_api_poi_search.py
autonomous: true
requirements: [GEO-01, GEO-02, GEO-03, GEO-04, GEO-05]
must_haves:
  truths:
    - "Tests describe the full Nominatim provider + reverse + POI contract"
    - "All tests FAIL initially (red) because implementation does not yet exist"
    - "Tests use the project's AsyncClient + ASGITransport pattern (see tests/test_api_tiles.py)"
  artifacts:
    - path: "tests/test_nominatim_provider.py"
      provides: "NominatimGeocodingProvider unit tests (geocode success, no-match, HTTP error)"
      min_lines: 80
    - path: "tests/test_api_reverse_geocode.py"
      provides: "GET /geocode/reverse contract tests (happy, no-match 404, service unavailable 503)"
      min_lines: 80
    - path: "tests/test_api_poi_search.py"
      provides: "GET /poi/search contract tests (lat/lon+radius, bbox, mutually-exclusive 400, no results)"
      min_lines: 120
  key_links:
    - from: "tests/test_nominatim_provider.py"
      to: "src/civpulse_geo/providers/nominatim.py"
      via: "import NominatimGeocodingProvider (will fail until Plan 02)"
      pattern: "from civpulse_geo.providers.nominatim import"
    - from: "tests/test_api_reverse_geocode.py"
      to: "src/civpulse_geo/api/geocoding.py"
      via: "httpx AsyncClient GET /geocode/reverse"
      pattern: "/geocode/reverse"
    - from: "tests/test_api_poi_search.py"
      to: "src/civpulse_geo/api/poi.py"
      via: "httpx AsyncClient GET /poi/search"
      pattern: "/poi/search"
---

<objective>
Write failing tests (RED phase) that describe the full Phase 26 contract: NominatimGeocodingProvider unit behavior, `GET /geocode/reverse` endpoint, and `GET /poi/search` endpoint. Tests drive implementation in Plans 02-05.

Purpose: Lock in the contract before building. Executors in later plans write the minimum code to turn these tests green.
Output: Three failing test files using project's established AsyncClient + ASGITransport pattern and `unittest.mock.AsyncMock` for httpx stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-nominatim-provider-reverse-geocoding-poi-search/26-CONTEXT.md
@tests/test_api_tiles.py
@tests/test_census_provider.py
@src/civpulse_geo/providers/schemas.py
@src/civpulse_geo/providers/base.py
@src/civpulse_geo/config.py

<interfaces>
From src/civpulse_geo/providers/schemas.py:
```python
@dataclass
class GeocodingResult:
    lat: float
    lng: float
    location_type: str      # "ROOFTOP" | "RANGE_INTERPOLATED" | "GEOMETRIC_CENTER" | "APPROXIMATE" | "NO_MATCH"
    confidence: float       # 0.0 to 1.0
    raw_response: dict[str, Any]
    provider_name: str
```

From src/civpulse_geo/providers/base.py:
```python
class GeocodingProvider(abc.ABC):
    @property
    @abc.abstractmethod
    def provider_name(self) -> str: ...
    @abc.abstractmethod
    async def geocode(self, address: str) -> GeocodingResult: ...
    @abc.abstractmethod
    async def batch_geocode(self, addresses: list[str]) -> list[GeocodingResult]: ...
```

Nominatim upstream JSON contract (to be mocked):
- GET /search?q=...&format=json&limit=1 → `[{"lat": "33.7", "lon": "-84.3", "display_name": "...", "place_id": 123, "type": "house"}]`
- GET /reverse?lat=...&lon=...&format=json → `{"lat": "33.7", "lon": "-84.3", "display_name": "123 Main St, Atlanta, GA", "place_id": 456}`
- No-match: empty list `[]` for /search; `{"error": "Unable to geocode"}` for /reverse
- GET /status → "OK" text or JSON, HTTP 200
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write NominatimGeocodingProvider unit tests</name>
  <files>tests/test_nominatim_provider.py</files>
  <behavior>
    - test_geocode_success: /search returns [{"lat":"33.749","lon":"-84.388","display_name":"Atlanta","place_id":1,"type":"city"}] → GeocodingResult(lat=33.749, lng=-84.388, provider_name="nominatim", confidence>=0.7, location_type != "NO_MATCH")
    - test_geocode_no_match: /search returns [] → GeocodingResult(lat=0.0, lng=0.0, location_type="NO_MATCH", confidence=0.0, provider_name="nominatim")
    - test_geocode_http_error: httpx raises RequestError → ProviderNetworkError raised
    - test_geocode_http_500: upstream 500 → ProviderNetworkError raised
    - test_provider_name: provider.provider_name == "nominatim"
    - test_batch_geocode_serial: batch_geocode(["a","b"]) calls geocode twice, returns list of 2 GeocodingResults in order
  </behavior>
  <action>
    Create tests/test_nominatim_provider.py. Import pattern like tests/test_census_provider.py. Use httpx.AsyncClient mocked with AsyncMock(spec=httpx.AsyncClient). Each test builds a MagicMock(spec=httpx.Response) with status_code and .json() returning the fixture dict. Hit URL `{settings.osm_nominatim_url}/search` with params q=address, format=json, limit=1. Use pytest.mark.asyncio. Import from civpulse_geo.providers.nominatim import NominatimGeocodingProvider — this import MUST fail now (module does not exist). Import civpulse_geo.providers.exceptions.ProviderNetworkError for error tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_nominatim_provider.py --collect-only 2>&amp;1 | grep -E "(6 tests collected|ModuleNotFoundError.*nominatim)"</automated>
  </verify>
  <done>File exists with 6 test functions; collection shows either "6 tests collected" OR ModuleNotFoundError for civpulse_geo.providers.nominatim (both acceptable — red phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write GET /geocode/reverse contract tests</name>
  <files>tests/test_api_reverse_geocode.py</files>
  <behavior>
    - test_reverse_happy_path: GET /geocode/reverse?lat=33.749&lon=-84.388 with mocked http_client returning {"display_name":"123 Peachtree St, Atlanta, GA 30303","lat":"33.749","lon":"-84.388","place_id":42} → 200, JSON body {address:"123 Peachtree...", lat:33.749, lon:-84.388, place_id:42}
    - test_reverse_no_match_returns_404: upstream returns {"error":"Unable to geocode"} → 404 with detail mentioning "No address found"
    - test_reverse_service_unavailable_503: when app.state.providers lacks "nominatim" key → 503 with detail "Nominatim provider not registered"
    - test_reverse_invalid_lat_422: GET /geocode/reverse?lat=999&lon=0 → 422 (FastAPI Query validation for lat in -90..90)
    - test_reverse_missing_params_422: GET /geocode/reverse (no lat/lon) → 422
  </behavior>
  <action>
    Create tests/test_api_reverse_geocode.py using AsyncClient + ASGITransport pattern from tests/test_api_tiles.py. Fixture `patched_nominatim_client` replaces app.state.http_client with AsyncMock(spec=httpx.AsyncClient). Assert GET is called with URL ending `/reverse` and params containing lat, lon, format="json". For 503 test, pop "nominatim" from app.state.providers (or ensure absent). Expected upstream URL: `{settings.osm_nominatim_url}/reverse`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_reverse_geocode.py --collect-only 2>&amp;1 | grep "5 tests collected"</automated>
  </verify>
  <done>File exists, 5 tests collected. Running them produces failures (route does not exist yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write GET /poi/search contract tests</name>
  <files>tests/test_api_poi_search.py</files>
  <behavior>
    - test_poi_lat_lon_radius: GET /poi/search?q=coffee&amp;lat=33.749&amp;lon=-84.388&amp;radius=1000 with upstream returning list of 3 POIs → 200, response.results has 3 items each with {name, lat, lon, type, address}
    - test_poi_bbox: GET /poi/search?q=park&amp;bbox=-84.5,33.6,-84.3,33.8 → 200, upstream called with viewbox=-84.5,33.6,-84.3,33.8 and bounded=1
    - test_poi_both_bbox_and_latlon_returns_400: GET /poi/search?q=x&amp;lat=1&amp;lon=2&amp;bbox=1,2,3,4 → 400 detail mentions "mutually exclusive"
    - test_poi_neither_bbox_nor_latlon_returns_400: GET /poi/search?q=x → 400 detail mentions "lat/lon or bbox required"
    - test_poi_no_results_empty_list: upstream returns [] → 200 with results=[]
    - test_poi_bbox_malformed_422: GET /poi/search?q=x&amp;bbox=not,a,valid,bbox → 422 or 400 (FastAPI/custom validator)
    - test_poi_service_unavailable_503: when "nominatim" missing from app.state.providers → 503
    - test_poi_missing_q_422: GET /poi/search (no q) → 422
  </behavior>
  <action>
    Create tests/test_api_poi_search.py using AsyncClient + ASGITransport. Fixture replaces app.state.http_client with AsyncMock. Upstream fixture response for search list is e.g. [{"display_name":"Starbucks","lat":"33.75","lon":"-84.39","type":"cafe","place_id":1}, ...]. Assert upstream URL is `{settings.osm_nominatim_url}/search` with params q=..., format="json", limit=50. For bbox test also assert viewbox and bounded=1 params. For radius test, assert params include lat, lon, and either a radius-derived bounded box OR the raw lat/lon (document in Plan 05 which approach is used — upstream Nominatim does not support radius directly, so radius is converted to a bbox degree approximation; test asserts viewbox param is present).
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_poi_search.py --collect-only 2>&amp;1 | grep "8 tests collected"</automated>
  </verify>
  <done>File exists, 8 tests collected. Executions fail (route /poi/search does not exist yet).</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_nominatim_provider.py tests/test_api_reverse_geocode.py tests/test_api_poi_search.py --collect-only 2>&1 | tail -5
uv run ruff check tests/test_nominatim_provider.py tests/test_api_reverse_geocode.py tests/test_api_poi_search.py
</verification>

<success_criteria>
- All three test files exist and are syntactically valid Python
- Test collection reports 6 + 5 + 8 = 19 tests (or module-import errors for nominatim provider, which become resolved in Plan 02)
- Ruff passes with zero warnings
- Tests fail when run (red phase) — target for later plans
</success_criteria>

<output>
After completion, create `.planning/phases/26-nominatim-provider-reverse-geocoding-poi-search/26-01-SUMMARY.md`
</output>
